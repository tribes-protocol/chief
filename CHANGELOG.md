- **fix(sidebar): the rail is built with the cursor and hands it straight back.** A bare `chief` opened the company with the cursor in the sidebar instead of in the person's pane, so the first thing the operator typed went to furniture. Both rail-mint sites — the attach frame (`attach.rs::rail_mint_argv`) and the actuator's repair batch (`actuate/interpret.rs`) — run `split-window` with no `-d`, which is deliberate: every write that follows names a WINDOW, and tmux resolves a window target to its active pane, so the new rail must be active while it is tagged and resized. The frame therefore ends, and only ends, with `select-pane -l` at that window: the last pane, which a split makes the pane that was active before it — measured on tmux 3.7c, and true even when the split targeted some other pane, so the cursor returns to the operator and never to the split target.

  Pinned at both sites by an argv test that fails if the selection is dropped or moved ahead of the tag, and by two REAL-tmux tests that ask the server which pane is active after the frame runs (`a_real_rail_mint_leaves_the_person_pane_active`, `real_rail_repair_leaves_the_person_pane_active_and_not_the_rail`). Both were run as controls with the selection removed and both fail there, reporting the rail as the active pane — which is the operator's report reproduced exactly.

- **chore(deps): the three open dependabot branches land as one batch, with the lockfile they never carried.** The npm minor-and-patch group (`@typescript-eslint/*` 8.67.0 to 8.68.0, `eslint` 10.9.0 to 10.9.1, `@types/react-dom` 19.2.4 to 19.2.5, `typebox` 1.3.16 to 1.3.18), `jsdom` 29.0.2 to 30.0.1, and `eslint-plugin-simple-import-sort` 12.1.1 to 14.0.0 are merged together on one branch, because three separate merges of the same lockfile serialize into three conflicts that say nothing.
  **Dependabot edited only the manifests.** Every one of its three commits touches `package.json` files and no `bun.lock`, and every CI job installs with `--frozen-lockfile` — so each of those branches was individually unmergeable and would have failed at the install step before running one test. The lockfile is regenerated once here, over all three bumps at the same time, and the frozen install is verified against it.
  **The instrument was checked against the lockfile, not remembered.** `eslint --version` reports 10.9.1 in the tree that lints, which is the check that separates a real lint result from a confident answer given by the wrong binary.

- **fix(install): a failed Pi install exits nonzero, like a declined one.** The
  script refused to call a DECLINED upgrade success — "a zero status would tell
  a script that everything is ready when the thing that runs people is too old"
  — and then reported a FAILED install as success anyway, through two `|| true`
  sites. A caller got the happy outro and status 0 with the agent runtime
  absent, which is strictly worse than the too-old Pi the decline path already
  refuses. **The rationale proved more than the code implemented.** Both sites
  now end in the same deliberate family as the decline, naming what did not
  happen and the command that fixes it.

  **And the rule now has an instrument, which it never had.** Every Pi branch
  was reachable only by running the real installer against the real network, so
  nothing checked the exit code any of them produced and the rule shipped as a
  comment. `installer-pi-exit-codes.test.mjs` runs the script's OWN bytes —
  extracted at a marker, executed against stub `pi` and `npm` — over five
  behaviours, and carries a control that restores `|| true` in a copy and
  asserts the block goes green, so the passing assertions are known to measure
  the defect rather than merely to pass.

- **feat(install): the installer installs and upgrades Pi instead of printing a command and hoping.** An absent Pi is installed without asking — chief cannot run a single person without it, so there is nothing to weigh. A Pi that is merely too old is somebody's working tool, and replacing it is a different act, so that one asks: `Upgrade Pi to >= <floor>? [Y/n]`, default yes. Declining exits nonzero and says why, because telling a script that everything is ready when the thing that runs people is too old is worse than failing.
  **The floor is read, never restated.** It has one definition in the Rust source, the release process already stamps it into the manifest, and the installer reads it from the manifest it has just unpacked. A version bump needs no edit to the installer, and the repository's single-definition guard stays satisfied — a number copied into a shell script would be a second definition wearing a copy's clothes.
  **The prompt reads the terminal, not its own source.** This file is piped into `sh`, so stdin is the script text; reading stdin would eat the rest of the installer. Where there is no terminal at all — CI, a container build — the default is taken and SAID, because a silent choice made on somebody's behalf is what surprises them later. Missing `npm` refuses cleanly rather than half-installing, and a Pi that npm reports as installed is CHECKED against the floor rather than announced as ready.

- **feat(intercom): organization mail rides the same queue as the operator's own typing.** A busy person now reads an ordinary message at the next step boundary inside the running turn, instead of at the end of it. The operator typing mid-turn has always been submitted as a steering message and consumed within seconds; ordinary mail rode the follow-up queue, which Pi consumes only once the agent has no more tool calls or steering messages — so somebody an hour into a piece of work did not see a teammate's message until the hour was over, while the identical words typed by the operator arrived immediately.
  **The digest is kept; only its consumption point moves.** Batching is still the answer to twenty messages arriving in one turn, and nothing about how a batch is built has changed.
  **The idle and boot rows are byte-identical.** Mail arriving at somebody doing nothing still starts them; mail arriving inside the boot window is still parked. This changes WHEN a busy person reads a message, never whether an idle one is woken — and it touches nothing in the converge, activity or settle paths, so the operator wake lease is untouched by construction.
  **The change was unpinnable as written, so it was made pinnable.** The delivery table was already covered, but which mode the mailbox passed into it was two string literals at two call sites — so the timing of every delivery in the product could change without one test noticing. It is one named decision now, and a test asserts the rule rather than the table.

- **fix(hire): the guidance told agents to omit the department id; the schema required it; the model obeyed the schema and guessed.** `org_hire`'s `departmentId` was a REQUIRED field whose own description opened "DEFAULT: the department YOU head". An agent read that, reasoned correctly that it should omit the field, met a schema that would not permit it, and improvised the most salient name in context — the company's. It obeyed the instrument over the claim, which is the right thing for it to do.
  **The field is optional now and the default is real.** An omitted id resolves to the department the caller heads, or failing that the one they sit in — the resolver that already existed and is word-for-word what the description promised. The promise was always implementable; it simply was not implemented. The worked example shows the omitted form first, because an example that keeps passing the field re-teaches the habit the description is trying to correct.
  **The company name is still refused on the override path**, and that refusal is now the only thing standing behind an explicitly-passed id, so it is pinned by a test that passes the field deliberately rather than omitting it. Accepting the name as an alias was rejected again and for a sharper reason than before: with a default in place the alias would exist only on the override path, where a mid-level head naming the company means their OWN team — so it would resolve to the impressive-sounding wrong target rather than the semantically right one.
  **A sweep asked the general question of every parameter in the file** — does the schema permit what the prose promises? — across `DEFAULT`, `omit`, `omitted`, `optional` and `leave empty`. Exactly one leaf field disagreed, this one. One other hit reads "never omit this field" and is required, so prose and schema agree. A mechanical guard was therefore not added: an instrument for a class of one is furniture.
- **feat(cards): a refusal says "refused"; a crash still says "failed".** The word is a claim about whose fault the failure was, and the two invite opposite recoveries — "refused" invites a corrected call, "failed" invites a retry. So `Hiring teammate refused` when the caller named a department that does not exist, and `Hiring teammate failed` when something actually broke. This is only implementable because the classification exists to follow; the verb reads it rather than matching on message text, which this file bans.
  **The split follows the classification, so a refusal added next month gets the right word without anyone remembering to add it.** One predicate, in one place, because three renderers build a failure title and a rule copied three times is three rules waiting to disagree — two of them were bespoke titles that would otherwise have kept saying "failed" for a refusal.
  **The `(system fault)` tag reads the same marker.** It measured only the ABSENCE of a status, so the verb moved to the fault marker and the tag stayed on the old instrument — a partial batch wrapping a real crash said "failed" correctly and then dropped the crash marker, leaving somebody debugging it to read a list of people already hired and reasonably conclude they had passed bad input. One classification, two surfaces, and a test that fails if either moves without the other.
  **One case turned out not to be what the classification said it was.** A partial hire carries a status so the card can name the people already hired, and a retry does not double-hire them — but the error it wraps may be a genuine crash. A status carried for CONTEXT is not a claim about fault, so that path now asks the wrapped error's own type, and a producer in the same position marks itself rather than being enumerated in the renderer. Without it, a real crash mid-batch would have been labelled a refusal and invited a correction to a call that was never wrong — the earlier defect pointed the other way, and worse for it.

- **fix(install): the installer no longer prints a curl error on the first line a stranger runs.** A clean install printed `curl: (23) Failure writing output to destination` immediately under "Resolving the latest chief release…", and then completed successfully. The install was never broken; the message was, and it appeared on the very first command anyone runs against this project — where a reader has no reason to read it as anything but a failure.
  **The cause was a pipeline, not the request.** The release lookup piped curl into `grep -m1`, which exits on its first match; if curl is still writing when it does, curl's write fails and it says so on stderr. The tag had already been captured, which is why everything downstream worked. Whether it appeared at all depended on whether the response outran the pipe buffer, which is why it was intermittent rather than constant — and why it is the kind of thing that reaches a stranger before it reaches the people who built it.
  **The installer also puts chief on your PATH itself**, instead of printing a line to copy. It writes to `~/.bashrc` and `~/.zshrc` when they exist, creates the one matching your shell when neither does, and **names every file it touched** — a script that edits somebody's dotfiles and does not say which is asking to be distrusted. Running it again changes nothing: a profile that already exports that directory is recognised and left alone, matched on the PATH rather than on an exact line, so a hand-edited variant counts as done.
  **And it does not claim the PATH is live when it is not.** `curl … | sh` runs in a child shell and cannot alter the shell you are sitting in, so the installer says that new shells will pick it up and gives you one command to paste for the current one. Where it wrote nothing — an unwritable profile, or a line already present — the original advice stands unchanged.
  **The response is saved and then read**, so there is no reader to exit early and no write to fail. That also makes a genuine failure honest: a request that really cannot reach GitHub now reports its own error instead of having it swallowed by the pipeline.

- **fix(cards): a deliberate refusal no longer reports itself as a system fault.** Naming the company where a department id belongs produced a card tagged `(system fault)` — for a refusal the tool had decided, worded carefully, and could explain. **A refusal that lies about whose fault it is invites the wrong recovery: a system fault invites the same call again, a caller error invites a corrected one.** An agent told the former will retry the identical call, because that is what the label means.
  **One mechanism, a whole class.** Card rendering classifies by whether a result carries a `status`: with one it is a named business-rule refusal, without one it is a raw caught exception. That distinction is right, and the throw path defeated it — validation refusals were plain `Error`s, and every adapter that catches a throw flattened it into a status-less result. So every refusal reaching the renderer that way was labelled a crash. Refusals now travel as a typed error carrying their own status, because this file forbids a second parser and classifying by matching message text would be exactly that.
  **The tail took three sweeps, and the shape is worth naming.** The surface pair of a duplicate-message refusal was converted while its twin one layer down in the mailbox writer survived — the same caller mistake, reached by the same tool, still tagged a system fault. Converting a refusal at the boundary does not convert the plumbing behind it, and the two read identically. Thirty-three sites in total.
  **The plain `Error` still means what it says.** An invariant no input should reach — a malformed reply, an unparseable authority file — is a system fault, the tag on it is correct, and retrying it is the right recovery. Both halves are pinned, because a test that only checked the first could be satisfied by giving every failure a status and deleting the distinction rather than fixing it.
  **And the company-name confusion is prevented rather than accepted.** Every `departmentId` parameter now says that a company name or slug is never a department id and where the real one is. Accepting the alias was rejected: it would bind at every `departmentId` parameter, so the same category error would reach `org_remove_department` and act on the root department — the wrong-target-from-name-confusion class, arriving through a convenience. The corrective hint also leads with the id now, so the one token a corrected call needs survives the collapsed card's 120-character window instead of falling just outside it.

- **fix(cards): card icons come from one reviewed table, chosen by codepoint property rather than by rendering on the author's machine.** An operator reported a card icon drawn as tofu, or overdrawing its neighbour, while the tick beside it was perfect. The cause is not the font: `🏗️` is U+1F3D7 BUILDING CONSTRUCTION plus a variation selector, and U+1F3D7 is a TEXT-default codepoint. Every wcwidth-based terminal — tmux included, and tmux is always in this stack — gives it one column while an emoji font draws two, and many font chains carry no text-style glyph for it at all because it exists almost only in colour-emoji fonts. The `✅` beside it is `Emoji_Presentation=Yes` and `East_Asian_Width=Wide`, which is why it was always fine.
  **Every card glyph now lives in `CARD_GLYPHS`**, admitted only when the terminal and the font agree on its width and it is old enough to be in out-of-date font chains. Six text-default glyphs are replaced, two codepoints newer than 2019 are replaced, and a power symbol with patchy coverage is dropped in favour of the words beside it. Bare drawing characters are a separate list, because the emoji rule is the wrong rule for them and a variation selector is what would break them.
  **Two icons that meant two things now mean one each.** The wave said both hello and goodbye — the same glyph on hire and offboard — so offboard becomes a door. Construction covered adding a department and starting two different things; a department is an office and starting is a rocket. Pause covered pausing and stopping. Stopping a reminder gets its own bell.
  **A guard makes the next one impossible to add quietly:** it sweeps every extension for characters in the emoji ranges and refuses any that is not in the table, naming file, line and codepoint — so an arbitrary glyph is caught on the day it is written rather than at the next audit. It carries its own negative self-tests, because a sweep that cannot fail proves nothing.

- **fix(naming): the sibling branches, found by sweeping rather than by list.** The first sweep converted one arm of several two-arm results and left the other: the ordinary bench result beside the converged one, the already-active recall beside the recall roster, the stand-down beside the start, the appoint-without-offboard beside the appoint-with. Also converted: the tool-call FAILURE and SUCCESS card targets, the batched roster prose on staffing cards, the session-maintenance and resume card targets, and the head-replacement prompt that named an incumbent by key. All of them take the same shape as the arm already converted beside them, which is what made them easy to miss and easy to check.

- **fix(naming): the remaining surfaces that named people by key.** A sweep after the first naming change found a family rather than a straggler, all of it the class the ruling names — text an agent reads and copies from. Converted: the provider-failure mail bodies that land in somebody's inbox as prose; the wake guidance returned to a sender mid-send ("… has departed; reroute the durable message"); the staffing and lifecycle tool results that read as sentences about people (bench, recall, start, transfer, offboard); the health-notice card's affected-person line and its idle-transition wait; and the tool-call card targets.
  **The mention colourer now resolves a username as well as an id.** Without it, every surface that started naming people properly would have quietly lost its identity colour — the colour is how a reader tells one person from another at a glance, so a naming fix that removed it would have traded one legibility problem for another. Ids still resolve first and exactly, so a username that happens to equal a different person's id can never take their colour.
  **Deliberately NOT converted, and recorded rather than left silent:** validation and authority errors that echo an id-typed parameter back at the caller. Those tools take a person id as their contract, so echoing the argument somebody typed is a key-echo of a key-typed input rather than a display of a name. Converting them widens into the staffing tools' parameter guidance and belongs to its own change.

- **feat(rail): the running version sits at the right of the rail's bottom row.** The operator asked for it after spending an hour telling two releases apart by hand. It is right-aligned with one column of padding, drawn in the same dim treatment as the collapse control beside it, and it is a status detail rather than a headline.
  **It is the version the running process reports**, the build-time constant `chief --version` prints — not a number read from an install manifest, which can disagree with the binary actually drawing the rail and would recreate the exact confusion the label exists to end.
  **A rail too narrow for both keeps the control and DROPS the version.** Truncating would be worse than omitting: `v0.5` is not a shorter spelling of `v0.5.2`, it is a different release, and a label that can misreport the running version defeats its own purpose. The two are also never allowed to touch — at least one column always separates them, so they read as two facts rather than one corrupted token.

- **feat(naming): the USERNAME is how people are addressed, everywhere a person is shown.** Operator ruling: *"Every time, use the USERNAME. That's how we communicate."* A person is three strings and they were being used interchangeably — the `id` is a kebab slug (`portfolio-management-head`) and is the durable key for mailboxes, document-store paths, transcripts and environment; the `name` is the roster display name; and the USERNAME (`@priya`) is derived from the name. The rule now enforced: the slug may key anything and must never be what a person or an agent is SHOWN.
  **This was not cosmetic.** Every surface an agent read named people by slug — the delivered message text, the inbox card, the sender line on a delivery — and the recipient resolver accepted only slugs, listing slugs in its unknown-recipient error. An agent shown a key addresses people by key, so a reply could be sent to a name nobody has, and delivered nowhere. Presentation and addressing were one defect and are fixed together: delivered messages and cards name the sender by username, and `org_send` accepts a username or an id. Two people who share a first name are REFUSED by name rather than guessed between, because guessing delivers somebody's message to the wrong person silently. The unknown-recipient error now lists usernames, with ids in parentheses, because that error text is guidance agents copy from.
  **The pane header is the role alone.** It used to read `@vera · Head of Quant`, putting one identity in the header while the footer showed a third spelling. The header is what you do; the footer is who you are.
  **The footer shows the username.** It rendered `@` plus the person id — showing the operator `@portfolio-management-head` — because the launcher published only the key. The launcher now publishes the username beside it as its own display-only variable, and the footer requires it: a pane that cannot name its person correctly renders no organization identity rather than naming it wrongly. The id keeps addressing the mailbox and the document store, unchanged.

- **fix(agent-home): a home that existed without its theme directory was warned about on every pass and repaired by none.** The home writer has two branches: one builds a home that is absent, the other refreshes a home that is already there. The refresh publishes the Chief-owned identity themes through a primitive that opens `<home>/.pi/themes` without following symlinks, and **nothing in the refresh branch created that directory** — only the create branch did. So a home that existed without it failed with `ENOENT` every single pass, while the warning printed above the call promised that the next pass would repair it. For this one error that promise was false. The failure also aborted before the role skill was installed, so such a person stayed roleless for as long as the loop ran.
  **How a home reaches that state, which is why it showed up on a fresh company.** Two callers build the same new person concurrently — the roster-mutation route and the converge pass. One creates the folder; the other, a moment later, sees a home that exists and takes the refresh branch against a home that is still being built. The company ends up correct, and the operator gets a warning per department about a problem that has already fixed itself.
  **The refresh now creates the directory it refreshes into**, so the warning's promise is true and a home orphaned by a crash mid-create heals on the next pass. The other interleave of the same race — both callers taking the create branch — is closed too: re-linking the `company` symlink to the target it already has is silent, while a link pointing somewhere else still fails, because that is a disagreement about the tree rather than a race.
  **A symlinked theme directory is still refused and is never followed**, with the refusal still coming from the trusted-parent check that owns that rule rather than from the repair.

- **feat(agent-home): chief stops managing Pi configuration — `PI_CODING_AGENT_DIR` is gone and Pi's own inheritance does the work (#1307).** Operator ruling, 2026-08-27: *"let pi do its own inheritance stuff"*, and on being told plainly that this means every person shares one settings file so anybody's `/model` moves everybody's default — *"that is fine. Don't copy on hire. default is fine"*.
  **TWO CONSEQUENCES YOU WILL FEEL, ahead of any mechanics.** First, **the default model is now SHARED company state**: any person's `/model` moves the default that every fresh session starts from, and sessions already running keep the model they are on. That is the operator's explicit choice, not a defect. Second, **agents now inherit the operator's EXTENSIONS as well as their config** — user-scope extensions are not trust-gated and `--no-extensions` has been retired, so anything in `~/.pi/agent/extensions` auto-discovers into every company pane. An extension installed "for all pi agents" now genuinely means all of them.
  **What changed.** Pi resolves its user configuration from `~/.pi/agent` for ANY working directory — `getAgentDir()` reads the env var when set and falls back to the home directory, and the cwd never enters that decision. chief was setting that env var to each agent's home, and that one line is what forced everything else: with config scope moved into the home, chief had to REBUILD the operator's configuration there, which it did with three symlinks. Every hazard those carried was downstream of a redirect Pi never asked for — links that dangled forever on a box with no `models.json` (#1283, whose reconcile this reverts as superseded rather than wrong), and a `settings.json` that Pi WRITES in place, so one agent's `/model` rewrote the operator's own file and moved every other agent's default. The redirect is gone; so are all three links.
  **What a home holds now**: `AGENTS.md`, a `sessions/` directory, a `company` symlink, the identity key, and PROJECT scope under `.pi/` — the one role skill at `.pi/skills/<role>`, the generated identity themes at `.pi/themes/`, and a `.pi/settings.json` that carries a theme and deliberately nothing else. Transcripts are the one thing genuinely per person, so they keep their own directory through Pi's own first-class `PI_CODING_AGENT_SESSION_DIR`. **The on-disk layout does CHANGE, and the reader was taught both shapes.** Pi's default session directory appends a `--<cwd-slug>--` component, but an EXPLICIT directory is used exactly as given — `const dir = sessionDir ? normalizePath(sessionDir) : getDefaultSessionDir(cwd)` — so a managed person's transcripts now sit directly in `sessions/` while the Chief's, chosen by Pi's default, stay slug-nested. `latest_session` scans both and takes the newest across them, so history written before this change stays findable and a restart still resumes the conversation it was having. Reading only one shape would have answered `None` for every managed person for ever, which presents as every restart starting fresh and reports as nothing at all, because `None` is also the correct first-boot answer.
  **The launch gate moved with it.** It asked each home whether `auth.json` or `models.json` resolved there; it asks the OPERATOR's own agent directory now. Same either/or rule, one answer for the whole company instead of one per person, and a refusal that names the directory the operator can actually fix.
  **`--approve` became load-bearing and its failure mode is silent.** Pi admits project-scope skills and themes only for a trusted project (`package-manager.js`, two `if (projectTrusted)` blocks). Since the role skill and the identity theme are now project scope, that flag is what DELIVERS them. Dropping it would not hang a headless agent on a prompt — it would launch every person with no role and no identity, with no prompt, no error and nothing in any log. The test that pins it is named for that consequence rather than for the mechanism.

- **docs: the README animation ends on the product, and the animation is the real company.** Two faults in one asset. The recording was stopped by detaching from tmux, so the last frames of the GIF were `[detached (from session org-northwind-robotics-849b1b_)]` on an empty screen — an artefact of how the capture was ended, not anything chief does, and the last thing a reader of the README saw. And the committed `docs/assets/panels.gif` was still the hand-composed MOCK it was always meant to be replaced by: the real capture — Northwind Robotics, four departments, eight people, recorded from a running company on 2026-08-26 — had never been landed, so the README had been showing an illustration of the product rather than the product. The cast is cut at the last product frame (the two tmux teardown events dropped) and re-rendered, so the animation holds the department sidebar at the end. `panels.cast` and `panels.gif` are updated together, deliberately: the source recording and the render must stay the same take, or the next person to re-render gets a different animation from the one in the README.

- **fix(agent-home): the inherited `models.json` link is reconciled, so it is present exactly while the operator's file is (#1283).** chief packages no `models.json` and writes none — a provider registry is Pi's concept and the operator's own artifact. The one touch chief had was a SYMLINK from each agent home into the operator's Pi agent dir, and it was written once, unconditionally, at create. Nothing but the operator's own hand ever writes that file, so on the ordinary box the link pointed at nothing: measured on a live company, every home under `.chief/agent/<person>/` carried `models.json -> /root/.pi/agent/models.json`, dangling, and the target had never existed.
  **The link is now reconciled on every pass**, in `agent_home::ensure_agent_home`, beside the role-skill and theme reconciles that already run there: present iff the operator's `models.json` resolves as a file, created when that file appears, removed when it goes. Reconciling rather than deleting the concept is the point — chief keeps MIRRORING operator model configuration and still never authors it, so an operator who writes the registry later has it reach every existing agent on the next pass, and dropping the link outright would have orphaned every agent from operator config permanently. `auth.json` and `settings.json` stay create-once: Pi writes both through ordinary use, so those links become live on their own.
  **What did NOT change, deliberately.** The launch gate's either/or in `converge_apply/resource_catalog.rs` — `auth.json` OR `models.json` resolving is enough — is untouched, so an operator who has signed Pi in and never written a registry still launches. #1283 also proposed refusing by name for any dangling inherited link; that would refuse exactly that operator, and is not done. The reconcile removes the dangling state without any refusal. A real `models.json` file placed in a home by hand is never deleted or overwritten: chief owns the link it wrote and nothing else.

- **fix(intercom): mail delivered inside the boot window is re-delivered when the window closes, instead of parking for ever.** Measured on a live company, 2026-08-27, down to a twice-queued envelope id: `message-queue-requested` for the same message at 02:07:54 and again at 02:09:37, **never consumed**. A delivery loop that never delivered.
  **The mechanism, which is arithmetic rather than a race.** Every fresh Pi session opens a ten-second boot window, and inside it every delivery becomes `deliverAs: "nextTurn"` — parked in Pi's `_pendingNextTurnMessages`, whose only reader is the next prompt submission. The deliveries all LAND inside it: the idle resume fires at one second, boot-time drains land seconds after subscription. The window's safety premise — chief always passes an initial message, so `agent_start` always arrives — is **false for a resume relaunch**, which comes back on old scrollback with no first turn at all. And `mailboxDeliveryAttempts` is released only at `agent_settled`, which needs that same absent turn, so the envelope was not even retried within the session. Each retry rode a fresh session with a fresh window: **every retry arrived through a door that was closed at the moment of arrival.**
  **The fix is that gate RESOLUTION now has a consequence**, on the fallback path only: release the delivery leases, drain through the ordinary busy/idle table so an idle pane gets `triggerTurn` and the turn starts, and re-drive the work-resume prompt if it was parked. No new clock — the fallback timer already existed; this is what happens when it fires. When `agent_start` DID come there is a real turn, the ordinary drain rides it, and the consequence never runs.
  **Two orderings are load-bearing and both are pinned.** The gate opens BEFORE the consequence runs, or the re-delivery asks a closed gate and parks exactly as it did on the way in — silently re-creating the livelock. And the leases are released BEFORE the drain, because a pane that never ran a turn has had no settle to clear them, so a drain that went first would find every envelope held by its own parked first attempt.
  **The work-resume prompt needed its flags reset, not merely re-driven:** `requestWorkResume` guards on `pending && !prompted`, and a prompt parked in the window left the opposite — prompted, with nothing pending — so a bare re-drive returns early and rescues nothing. Reaching the fallback is itself the proof that no turn consumed it, because `agent_start` clears that timer.
  **During the window nothing changes.** The `nextTurn` parking protects Pi's bare boot `prompt()` from a `triggerTurn` race, and its existing pins are untouched. **One cosmetic edge, judged rather than discovered later:** a parked copy may ride the triggered turn, so an envelope can present twice in that first turn — Pi 0.80 does not collapse the two queues and chief cannot reach into either. Twice-shown beats never-shown, and durable acceptance is unaffected because it keys on `message_start`. Pinned by `runs the re-delivery consequence when no first run ever arrives`, `opens the gate BEFORE running the consequence, or the rescue re-parks`, `never runs the consequence when a first run did arrive`, and `re-drives a work-resume prompt that was parked, and only that one`.
  **What those four tests do NOT cover, said here so a green board cannot imply it.** They pin the RULES the consequence embodies — that it fires on the fallback path and never on `agent_start`, that the gate is open before it runs, and which parked prompt needs re-driving. **The end-to-end WIRING — that the consequence reaches the real drain, the real leases and the real resume — is review-verified, not test-verified.** `drain`, `requestWorkResume` and `mailboxDeliveryAttempts` live inside the install closure, and the only harness that installs this extension drives no Pi lifecycle at all (*"no lifecycle is delivered: a declaration needs no session"*), so nothing existing can drive `session_start`. Building that harness is #1304; it was not bolted onto an outage fix, because an instrument built in anger is the failure class this repository spent the same night cataloguing.
  **And one comment that cost two people an hour**: `recordDrainGate` records only GATED drains, so a clean drain is deliberately traceless in `mailbox.jsonl` — a silent mailbox log does not mean no drain ran, it usually means every drain was clean. The delivery trace lives on the bus. Said in the code rather than fixed by logging every clean drain, which is the volume the edge-triggering exists to avoid.
- **docs(agents): CORRECTION to the singleton entry — the "live company belonging to a stranger" was the reporter's own daemon on a reused port.** The rule and its remedy are unchanged and still worth the entry: beacond is box-wide and singleton, `ensure_running` makes the starter arbitrary, starting one confers no ownership, and you ask what it is serving before stopping it. **The near-miss is downgraded to what the measurement actually showed** — a registry holding a row for a STOPPED company, not one command from somebody else's outage.
  **The half that is now the entry's centre is the half that did the fooling: a stale rendezvous plus PORT REUSE makes a dead company answer a health check.** The stranger's row and `daemon.json` named a URL and a pid; the pid was long gone, and the reporter's own company had since bound that same port. Curling the URL returned a healthy daemon — their own — and it was read as somebody else's live company. One `ps` on the recorded pid settled it.
  **So the remedy is explicitly NOT "curl the URL", because that is the instrument that lied.** Check the recorded pid, or ask the product, which already guards this exactly: the rendezvous parser refuses a record that does not describe the directory it was read for, the daemon start loop refuses to adopt a published pid it did not spawn, and `pid_is_live` exists for this question alone. A person reading a URL out of a registry listing has none of those guards, and that gap is the finding.
  Recorded as a correction rather than an edit, and stated plainly, because **an entry that overstates its evidence is worse than no entry** — the next reader calibrates on the strength of the claim.
- **docs(assets): the README's GIF and stills are a capture of a running company, not a composite.** Operator ruling — the old asset showed chrome their box does not have, and only the rail in it was ever real: everything to its right was hand-composed in the repo's card style, because a company could not be booted on the capture box at the time. **The measurement is the whole difference: the committed cast carried 9 events over 22 seconds; the new one carries 1,440 over 55.** Nine events is a drawing. A recording of a terminal is not nine events.
  **What is in frame is a live company**: `chiefd` and beacond running, a CEO and three departments with heads and members, six Pi agents answering in character, and a paused department whose people show the red asleep glyph the caption describes. Every gesture is a real SGR mouse click on the rail — departments expanding, a person's panel opening, the sleeping department opening its own window. Both stills come out of that same recording rather than being shot separately, so they cannot disagree with it.
  **Two product defects were found by trying to make it honest, and both were fixed before the capture rather than edited out of it.** A department card's pane carried no border title, so tmux drew its default — the machine's hostname — above the card on every box; and enabling that border globally had no global format, so any not-yet-titled pane showed the host for the instant before a title reached it. The capture went from 95 occurrences of the box's name, to 6, to **zero**, by changing the product twice. **Nothing was cropped, retouched or staged**: the Pi update banner and the `csi-u` warning are in frame because they were on the screen, and were checked for private content rather than removed for being untidy.
- **test(rendezvous): the real writer's bytes now reach the real parser, in the only lane where they can.** The fix for the crash-loop landed in #1289; this closes the seam that let it ship green. Nothing anywhere fed `parseDaemonRendezvous` bytes a real chiefd had written — `publishDaemonRendezvous` hand-authors exactly the four fields the reader consumes, under a comment saying the file goes "where the daemon writes it": **where, not what.** The writer under test and the reader under test were the same program agreeing with itself about a foreign surface.
  **Why it was never simply an oversight, which is the finding.** `startCompanyDaemon` boots `chiefd run --serve-only`, and that path returns at `run.rs:2957` into `serve_only_snapshot` — before the only `rendezvous::publish` in the file, at `run.rs:3513`. **Serve-only never writes a rendezvous, structurally.** The cheap harness every suite uses cannot produce those bytes at all, so the hand-authored fixture was not laziness; it was the only thing available. The pin therefore lives in a `toolcontract` lane, where a full `chiefd run` actually publishes, and it installs no tool surface because `installOrganizationToolSurface` republishes the rendezvous to re-point it at a proxy — legitimate there, fatal here, since it would overwrite the very bytes under test.
  **And a declaration parity test cannot stand in for it, which is not a hypothetical.** `RendezvousWireParity.test.ts` parses `rendezvous.rs` for the fields it DECLARES; `build` carries `#[serde(default, skip_serializing_if = "Option::is_none")]`, so the declaration and the bytes **already disagree conditionally, today, in this exact record**. Three readers of one declaration can agree with each other and all be wrong about what is on disk. The two instruments are complementary: parity catches struct drift at unit speed, this catches everything a source parse structurally cannot see.
  The suite asserts the parse and the four consumed fields, deliberately does NOT enumerate the daemon's keys — that would be the refused strictness relocated into a test — and carries a non-vacuity arm that goes red, loudly and self-explanatorily, the day the writer stops emitting more than four fields. `AGENTS.md`'s standing check list names four excluded suites now, with the reason the fourth exists.
- **fix(reminders): a recurring cadence inside the settle window is refused, and the floor is derived from the window it must clear.** Measured on a live company, 2026-08-27: a person woken about once a minute by a legally-armed recurring reminder, each turn correctly deciding the standing work needed no action, **$2.295 spent deciding nothing** — with the person held resident throughout. The arithmetic behind it: the cadence floor was 60s while the settle lease was 300s, so every fire delivered a turn, every turn reset the settle countdown, and **parking became unreachable out of entirely legal inputs.** The floor's own doc comment already named that hazard ("polling in a reminder's clothes … holding the fleet open"); the number beside it was five times too small for its own sentence.
  **The floor is now DERIVED, not written down twice:** `MIN_RECURRING_REMINDER_INTERVAL_MS = 2 * ORGANIZATION_SETTLED_IDLE_STOP_LEASE_MS`. Twice, so a whole park FITS between fires: at exactly one lease the fire races the park; at two the person settles, parks, and the next fire wakes a parked person — the designed mailbox-wake path. Derivation is the point rather than the multiple: the lease moved from 120s to 300s by operator ruling and this floor did not follow, because nothing said it had to.
  **It applies to RECURRENCE, which is where the hazard is.** The issue proposed one floor for every reminder; a one-shot cannot reset a countdown for ever — it delivers one turn and the person settles and parks — and its `interval_ms` is a delay, not a cadence. Forbidding "remind me in two minutes" would refuse a harmless request in the name of a hazard that cannot occur in it, and would make every live delivery test wait ten minutes to watch one fire. So the one-minute delay fence stays for one-shots and the new floor governs cadences, with both refusals saying which is which.
  **No migration, and the mechanism that makes that true is now a promise rather than a coincidence:** the re-arm clamps to the cadence floor, so every row armed under the old floor is corrected at its next fire. The document-validation fence deliberately keeps the DELAY floor — validating stored rows against the cadence floor would reject exactly the rows the clamp exists to migrate, and a rejected document is a company that will not load.
  **The floor had a second home in the TS arm tool's schema**, which is the two-copies-of-one-contract defect class this repository paid for a day earlier. Both values are now pinned by `ReminderFloorParity.test.ts`, which **resolves the Rust constants from source and evaluates the relation** — the Rust side is `2 * <lease>`, so a pin that matched a literal would sail past it green for ever. Pinned by `a_reminder_cadence_inside_the_settle_window_is_refused_at_arm`, `the_reminder_floor_lets_a_full_park_fit_between_fires`, `a_one_shot_inside_the_settle_window_is_still_allowed`, `a_legacy_sub_floor_reminder_is_clamped_at_its_next_re_arm` (`chiefd-core/src/store/supervision/reminders/tests.rs`) and the four parity tests.
- **docs(agents): starting a singleton does not make it yours, recorded from a near-miss that was one command from an outage.** beacond is box-wide, one process, on a fixed port, and `ensure_running` means the FIRST process that needs it starts it — so which seat started it is an accident of ordering. A seat finishing with its own throwaway company moved to stop the beacond it had started, asked what beacond held first, and found **another seat's company live and answering**, belonging to somebody who had no idea the first seat existed. The instinct "I started it, so I clean it up" is correct almost everywhere on a shared box and exactly inverted for a singleton.
  **The remedy is stated as a question rather than as care**, because "be careful with beacond" is unactionable and `GET /v1/list` is one command that answers precisely the thing you are about to be wrong about. It also records why stopping a company by its own verb matters beyond politeness: a pattern kill would have taken the stranger's company down **without ever revealing that it existed**, and a command that cannot be audited before it acts is the reason that ban is absolute.
  The same entry carries the session's third instance of a loose one-liner answering confidently and wrongly — a sweep that reported five surviving Pi processes had matched the VNC and desktop processes on incidental argv text, where a check against the executable name reported zero. One lesson, three instances, which is what makes it a rule rather than a story.
- **fix(sidebar): enabling the pane border globally now sets a global FORMAT with it, so no pane can fall back to the hostname.** #1295 titled the department card and took the leak from 95 occurrences in a capture to 6. The remaining six were **transient**: a department window's rail pane flashing `0 "<hostname>"` for the instant between its mint and the first title pass. Titling each pane kind fixes the kinds we know about; this fixes the ones we do not — a pane that is new, stray, or simply early.
  **The enabling site owns the fallback.** tmux's own default `pane-border-format` is `#{pane_index} "#{pane_title}"`, and `pane_title` for a pane nothing has titled is the machine's hostname — so turning the border on globally without saying what it says by default is precisely what made a hostname the default. Every site that sets `pane-border-status top` now sets the format beside it, and a test counts the two so a fifth enable site cannot be added without one.
  It renders **nothing** rather than a substitute name: an untitled pane has no honest title to give, and a blank border is the truthful answer where tmux's default is an accidental one.
- **fix(sidebar): a department card carries its own border title, so the operator's hostname stops being drawn above it.** `pane-border-status` is turned on GLOBALLY and every pane chief manages is given a `pane-border-format` — except the department card. tmux's default for an untitled pane is `#{pane_index} "#{pane_title}"`, and `pane_title` for a pane nothing has titled is **the machine's hostname**. So clicking a department — the product's central gesture — drew the operator's box name above the card, on every box, in every department window.
  **Found by trying to record the README asset**, which is the only reason anybody looked: the capture carried the host's name in 95 frames, and a README GIF would have published a box name to a public repository. Measured on a live company, five panes were inheriting the default — the three department windows, a placeholder, and the actuator's own.
  **The global `pane-border-status` is NOT the defect and is deliberately left alone.** The rail is titled with the company and every person pane with `@person · Role`; the chrome is intended and correct for them. The card was simply never given a format, so the fix is the missing title rather than the removal of a surface three other pane kinds rely on.
  Titled at all three sites a card pane comes into existence or is repainted — the mint, the refresh and the sleeping-pane restore — because a card that never changes is never refreshed, and titling only the refresh would leave a freshly minted card showing the hostname. The format reads `#{window_name}` rather than a threaded string: chief already names a department's window after the department, so the title cannot drift from it and no caller has to carry a display name it does not otherwise hold.
- **docs(agents): the guard-run wrapper this file prescribes could not report a failure, and now does.** The standing instruction was *"run it under `setsid nohup`, and check the exit code before you read the output … 144 is the tell"*. **`setsid` forks whenever it is already a process group leader — which it is, invoked from a shell — so the shell does not wait, and the exit code checked is `setsid`'s own: 0, whatever the run did.** The 144 tell the entry is built on could never fire in the form the entry prescribed. Measured both ways: `setsid nohup bash -c 'exit 7'` reports 0 and `setsid --wait nohup bash -c 'exit 7'` reports 7. Plain `nohup` waits but does not detach the session, which is what the SIGTERM problem needs; `setsid --wait` is the one form that both detaches and reports.
  **The second consequence is worse because it is silent: anything chained after a non-waiting run executes WHILE the run is going.** Measured cost the same day: a control run for a new guard — edit a file, run the guard, restore the file, all on one line — reported PASS twice, because the restore beat the guard to the file and the guard read the restored copy. The guard was genuinely blind to the defect it existed for, and its control said it was fine. So the entry now also says that a command which mutates state is run, and read, in separate invocations, whatever the wrapper. `scripts/orphanable-spawner-lib.mjs`'s `setsid nohup … &` is deliberately NOT changed: there the point is that the daemon outlives the script, so not waiting is the intent.
- **feat(rendezvous): the reader inventory on the surface's own doc, and a guard that keeps it honest.** The remainder of tonight's outage work. The rendezvous module doc said "`chiefd-daemon` WRITES it and `chief-cli` READS it" — a two-name inventory of a record with **three** readers. The third, `packages/chiefing/src/discovery/Rendezvous.ts`, announced itself only inside its own file, in another language, in a package no Rust change touches. When a field was added to that record — additive, compatible, reviewed — the unnamed reader refused it and every pane in a live company exited 1.
  **Nobody was careless, which is the whole point.** The change touched every reader anyone knew about; the reader that was not named was not looked for, because a surface's own doc is where an engineer goes to learn who consumes it, and this one answered with a number that was too small. **An incomplete reader inventory on a surface's own doc is how a cross-language contract loses a reader.**
  The doc now names one writer and three readers by REPOSITORY PATH — `chief-cli`'s `daemon.rs` and `stop.rs`, and the TypeScript parser reached by both `organization-intercom.ts` and `team-ui.ts` — and states, verified by grep rather than recalled, that `chiefd-daemon` writes this file and never reads it back. **`rendezvous-reader-inventory.test.mjs` makes the list enforceable rather than aspirational**: it finds every file that calls a rendezvous reader and fails, naming them, if one is missing from the doc. It greps for CALLS and not imports, because a barrel that re-exports a reader is not one, and it ignores prose — `types/Transport.ts` mentions `readDaemonRendezvous(dir).url` in a doc comment and reads nothing, which was this guard's first false positive and would have been the reason somebody deleted it.
  **Proven able to fail before it was trusted, twice, because the first two versions could not.** With the TypeScript reader dropped from the inventory the guard now goes red on both arms and names the missing file; restored, it is green. Version one passed that control — it excluded the parser file as "not a reader", when defining `parseDaemonRendezvous` is exactly what makes it the third one. Version two passed a DIFFERENT control: its blindness floor was aggregate (`readers.length >= 3`), and the three TypeScript callers satisfy three on their own — so renaming `read_rendezvous` would have dropped the Rust side out of the guard silently, leaving a TypeScript-only instrument reporting green. The floor is per language now, one sentence each, so a rename on either side is a red that names the side. **A guard whose control run passes is not a guard** — and a guard needs one control per thing it is supposed to see, not one control in total.
- **fix(discovery): a rendezvous reader ignores fields it does not know — an additive daemon field killed a live company.** #1282 taught the daemon to publish `build` on its rendezvous, which is additive, backward compatible and correct on the writer's side. The TypeScript reader in every person's pane refused the whole record: `Failed to load extension … unknown field "build" — this daemon is a different build`. Pi exited 1, every pane died at start-up, and the sentence named a version skew that was not happening. **An additive change to a writer became a cross-language outage purely because a reader was strict.**
  **A rendezvous is a RECORD somebody else writes, and forward compatibility is the correct posture for reading one.** What the parser owes its callers is that the fields it USES are present and well-formed — every one is still checked, and a rendezvous missing `dir`, `key`, `url` or `pid` is still refused, because that record is unusable rather than merely unfamiliar. What no reader owes anybody is an opinion about a field it has never heard of. Strictness there makes every future field addition an outage, and it lands on the reader furthest from the change. The misleading sentence goes with the check: an unparsable rendezvous and a genuine build mismatch are different findings with different remedies, and this parser is not what detects the second.
  **The identical trap is closed on the RUST reader in the same change**, because either side can be the old half: an old `chief-cli` reading a rendezvous from a newer daemon was the same outage one language over, waiting. `deny_unknown_fields` comes off `DaemonRendezvous` only — it stays everywhere it guards a REQUEST body, where refusing an unknown field catches a caller's typo rather than a writer's new fact.
  **The seam nobody tested is now tested.** The Rust side had a wire test, the TypeScript side had a parser test, each passed against its own idea of the record, and a real writer's output read by the real reader was covered by nobody — the same seam class as the packaging bug, one language boundary over. `RendezvousWireParity.test.ts` derives the writer's field list from the Rust struct (never a copy, which would drift exactly when somebody adds a field) and asserts this reader accepts a record carrying all of them, plus the literal record that caused the outage. Pinned by `accepts a rendezvous carrying a field it does not know`, `still refuses a rendezvous missing a field it depends on` (`Rendezvous.test.ts`, replacing the assertion that pinned the opposite), `a_field_this_build_does_not_model_is_ignored_rather_than_refused` and `a_rendezvous_missing_a_field_this_build_depends_on_is_still_refused` (`host-primitives/src/rendezvous.rs`).
- **feat(chief): the resident actuator says which build it is, and a live one on a replaced binary is restarted.** The last arm of the installed-build rule (#1281), after `chiefd` (#1282) and `beacond` (#1287). The actuator was the component whose staleness the operator actually saw: after a release, an actuator from 17:01 crash-looped to 164 failures while the fixed binary sat on disk and nothing said so.
  **It reports through a tmux SESSION TAG, which is this repository's existing doctrine rather than a new idea.** `.chief/` is not runtime placement and nothing on disk is: every fact about a live tmux object already lives on the object — a pane's person, its launch hash, a window's department — because nothing on disk can be trusted to still describe a live server. Which binary the process in that pane is running is the same kind of fact about the same kind of object, so it is stamped on the actuator's own session at start (`@chief_actuator_build`) and read from there. Best effort by construction: a tmux that will not take the option costs a build check, never an actuator.
  **The decision table gained one arm and one input, and the arm is distinct on purpose.** `ReplaceStale` is not `ReplaceExited`: the pane is ALIVE, there is no corpse to quote and no last words to rescue, and the operator is told a working actuator is being replaced and why — a company whose actuator restarts for no stated reason reads as a fault. The build decides the LIVE case only: a corpse is replaced because it is a corpse, an absent session is created, and a session tmux would not describe is still refused rather than doubled, whatever the build says.
  **And `Unknowable` never stops a live actuator** — one from a build predating the tag, one running out of a development tree, an unreadable install, or a tag this client cannot parse. It is still the actuator this company is being run by, and stopping it to satisfy an unanswered question takes a working company down. The comparison target is `current_exe()` rather than a separate install path, because the actuator IS this program: `resolve_actuator_command` starts it from `current_exe()`, so the build that should be running is the one the operator just invoked. Pinned by `a_live_actuator_is_replaced_only_when_its_build_is_provably_wrong`, which walks all four session states against all three build answers, and by the existing table test, whose assertions are kept and restated with the input the rule gained.
- **feat(chief): the box-wide beacond reports what it is, and a stale one is replaced once, loudly.** Lane two of the installed-build rule (#1281): the same ruling that covers `chiefd` covers `beacond`, and beacond was the component with no way to answer at all. `/v1/health` was `{"status":"ok"}` — no pid, no version, no database, no build — and `chief` spawns beacond DETACHED and drops the child handle, so the pid of the beacond a box is running was knowable to NOBODY. A rule that says "signal the pid it REPORTS, never one you inferred" had nothing to read.
  **So health now carries `{status, pid, version, dbPath, build}`**, measured once at start while the process's own executable certainly still exists — the same `ReportedBuild` shape the company daemon publishes on its rendezvous, so `chief` has one comparison to make and not two. It rides the health answer rather than a route of its own because the discovery ladder already probes health before it decides to spawn, so the check costs no extra request. The identity is reported on the UNHEALTHY answer too: a beacond whose schema is missing is exactly the one an operator has to identify, so identity is not the reward for being well. The queued database-path observability item folds in here as the same field set.
  **The blast radius is stated in the operator's own line, not only in a comment:** restarting beacond is box-wide and costs every company on the box its discovery for the moment it takes to rebind — while touching no company state and no running pane, because beacond is the presence registry and daemons and panes do not depend on it after admission. Stopping it is SIGTERM to **the pid beacond itself reported**; the respawn is the ordinary `ensure_running` spawn.
  **Unknowable never acts, and here that matters most.** A beacond that predates the field, one running out of a development tree, and an unreadable install are each left alone with one line — on the one box-wide component, acting on a question nobody answered would cost every company its discovery to satisfy a check. A health answer this client cannot parse reads as UNKNOWABLE, never as unreachable: read as unreachable, a live beacond would have a second one spawned over it by every command. One restart attempt, then a refusal naming both paths. Pinned by `a_rebuilt_beacond_is_stale_and_the_line_states_the_box_wide_blast_radius`, `a_current_beacond_is_not_restarted`, `a_beacond_that_reports_no_build_is_never_restarted`, `a_development_beacond_is_out_of_scope`, `the_health_answer_beacond_actually_writes_is_the_one_this_client_parses` and `an_older_beacond_answer_parses_and_reads_unknowable` (`chief-cli/src/discovery.rs`), and by `health_reports_the_pid_version_database_and_build_of_this_process` and `an_unhealthy_beacond_still_says_which_beacond_it_is` (`beacond/src/router.rs`).
- **feat(ci): the extensions of an installed release are loaded by real Pi, in a lane of their own.** Three of a live company's people were dead panes, `status=1`, with Pi's own words: `Failed to load extension ".../organization-intercom.ts": Cannot find module '@chief/piing/extension-runtime'`. The installed tree shipped the runtime FILES without the package IDENTITY its own extensions import them by. The packaging defect is fixed (#1284); this is the instrument that would have caught it, and that catches the next one.
  **Every instrument in it is one of the two that SHIP.** The tree is staged by calling the release's own `assembleVersionTree` — the same definition `publishVersion` and `scripts/package-release.ts` both use, invoked rather than mirrored — and it is judged by launching the real `pi` binary with `--extension`, the same loader a person's pane runs. That is the design rule and it disqualifies the cheaper version: **the defect lived in the GAP BETWEEN TWO RESOLVERS**, so a guard that introduces a THIRD — a hand-rolled specifier walk, or `import.meta.resolve` under the test's own runtime — is one more resolver that can disagree with Pi's, and *"it resolves for my resolver"* is exactly the look-implemented shape that shipped this.
  **It asserts both directions, because an absence is not a signal.** No module-RESOLUTION failure in Pi's output (`Cannot find module` / `ERR_MODULE_NOT_FOUND`, which are Node's words and the ones the dead panes carried) AND one run is shown reaching the intercom's own `ORG_LAUNCHER_RUNTIME_SOCKET and ORG_LAUNCHER_RUNTIME_SESSION must be provided together` — a line that executes only after module resolution finished, with the sentence read from the staged source rather than copied into the guard, so a reword fails loudly instead of passing silently. **And a CONTROL arm re-creates the shipped defect on demand:** identity removed, runtime files present, which is exactly what the release did — and asserts Pi answers with the operator's own error. An instrument that cannot be made to go red has not been shown to see anything, and with the fix already merged this is the only honest substitute for a red-first run.
  **It does not duplicate `installed-release-loads-its-extensions.test.mjs` and each says so.** That guard asks Node whether the specifier RESOLVES — cheap, in the ordinary guard shard, on every pull request, over a staged topology whose `dist` files it synthesizes because its lane installs `--ignore-scripts`. It cannot prove the module LOADS, that its exports are what the extensions destructure, or that Pi accepts it. This one proves that with a real build and a real Pi, and pays for a lane of its own to do it.
  **Its retirement condition is written in its own header**, because it is slow and earns that cost only while Pi's loader can differ from Node's resolver: a VERIFICATION that Pi delegates bare-specifier resolution to Node byte-for-byte retires this guard; an assumption that it does, or a reading of Pi's source nobody ran, does not. The defect this exists for was two resolvers believed to agree that did not, so it is deleted on evidence and never on confidence.
  **`pi -ne` appears in no remedy anywhere.** `--no-extensions` is used inside the guard purely to isolate the one extension under test; an extension-less person is a person with no intercom, so Pi refusing to start on an extension it cannot load is correct behaviour being correct. Wired by file through `guard-wiring-manifest.mjs` and it runs in the existing guard shard, which already provides both prerequisites — `ci-guard-shards.mjs` builds `packages/*` before sharding, and `--ignore-scripts` does not skip the `pi` devDependency. **A dedicated CI lane was written first and deleted**: it would have been a second place to keep those prerequisites correct, and the repo's own `ci-sequence` guard is right that a lane installing WITH lifecycle scripts is the wrong shape. It refuses in words, naming the missing thing, when pi, bun or a built `dist` is absent.
  **One thing its first CI run taught, recorded because it nearly shipped as a false green:** Pi wraps ANY throw from an extension's activation in the same `Failed to load extension "<path>": …` sentence, so matching that phrase conflates the outage (a module that could not be found) with its own proof of absence (a module that loaded, ran, and threw on purpose). The first version of this guard matched the phrase and failed on its own positive signal. Only Node's resolution words are the defect.
- **feat(chief): a stale daemon is replaced at start, and "different build" stops being a version string.** Operator ruling: *"when I run `chief` and chiefd/beacond/actuator/etc are on old shit, it needs to kill that shit if it's different."* After a release the running `chiefd` stayed on the old binary — an actuator crash-looped to 164 failures while the fixed build sat on disk and nothing said so. The H.6 skew guard REFUSES a mismatched daemon; it does not replace one, and it cannot see this case at all.
  **The key is `(device, inode)`, because the incident was a `0.5.0` -> `0.5.0` rebuild.** Same declared version, same install path, different bytes: a version comparison answers "same, leave it" and fails the exact case that motivated the ruling, and a path comparison fails it too because the path is the half that stayed the same. A re-release removes `versions/<v>` and writes a new file there, so the inode is what moves. `/proc/<pid>/exe` reading `(deleted)` is a true detail for the operator's line on Linux and deliberately NOT the test — it is one kernel's procfs presentation convention, and a check keyed on that string is one cosmetic change away from silently passing everything.
  **The identity is REPORTED, never inferred, which is what makes one rule work on both platforms.** Each component stats its own executable at start and publishes `{exe, identity}` beside its pid — `chiefd` on its rendezvous today. `/proc` does not exist on macOS, and the Darwin call that looks like its equivalent answers with the process's cwd and root vnodes rather than its executable (`proc_vnodepathinfo` is `pvi_cdir` + `pvi_rdir`), so a `/proc` design has no honest Darwin arm: it would either refuse there — leaving the operator's own incident class undetectable on their platform — or compile against the wrong vnode and look implemented while passing everything. A process can always stat its OWN executable, everywhere, at a moment when the file certainly still exists.
  **Only a PROVEN mismatch restarts anything.** A daemon from a build that predates the field, a component running out of a development tree, and a broken install are each UNKNOWABLE: said out loud once, never treated as stale. Treating "I do not know" as "replace it" would take working companies down on every start. The bootstrap generation closes itself at the next restart. **And the identity is four fields, not two, because its own red-first test proved inode reuse is not rare:** the first version keyed on `(dev, ino)` alone and CI answered `Current` for a genuinely replaced file — the filesystem handed the replacement the inode the removed file had just freed, which is exactly the workload a release produces. `(dev, ino, size, mtime)` is not mtime ARITHMETIC — nothing is compared for older-or-newer and no clock is trusted — it is more of the same fingerprint, and the residual it leaves needs a replacement to match on device, reused inode, byte length AND nanosecond timestamp at once.
  **The stop verb is `daemon::stop`, and never the `chief stop` teardown.** That path is eight steps with `kill-actuator` and `kill-session` ahead of `stop-daemon`, so a version check reaching for it would stop every person in the company to update a binary and would override an operator's own wake click. `version-ensure-uses-the-daemon-only-verb.test.mjs` pins the pane's survival as a property of the VERB, because both verbs are spelled `stop`, they live in sibling files, and the wrong one looks more thorough.
  **One attempt, then a loud refusal.** A rule that restarts on mismatch can restart for ever behind a `bin/` symlink into a stale version directory. The fresh daemon is checked once; still stale is a refusal naming both paths and asking whether the install has two roots. Pinned by `a_same_version_rebuild_is_stale_even_though_the_version_did_not_move`, `a_component_running_the_installed_build_is_current`, `a_component_from_an_older_version_directory_is_stale`, `a_component_that_reports_no_identity_is_unknowable_and_says_why`, `a_development_build_is_out_of_scope_and_is_said_out_loud`, `an_absent_install_is_unknowable_rather_than_stale`, `the_refusal_names_both_paths_and_refuses_to_loop` (`chief-cli/src/build_identity.rs`), `a_daemon_that_reports_no_build_is_adopted_rather_than_stopped`, `a_report_from_outside_a_versioned_install_is_never_a_restart` (`chief-cli/src/daemon.rs`), and the rendezvous wire tests in `host-primitives`. **beacond and the resident actuator are deliberately NOT in this change** — each needs its own report surface first, and half an arm that looks implemented is the failure this whole change is about.
- **fix(release): an installed release ships the package IDENTITY its extensions import, not just their files.** Every person in a live company crash-looped with a blank card until the pane's own stderr was harvested by hand: `Cannot find module '@chief/piing/extension-runtime'`. A checkout resolves that specifier through `node_modules/@chief/piing`, a workspace link `bun install` creates; an install has no `node_modules`, so the same import against the same source resolved against nothing and Pi exited 1 before the pane did anything. **This is the `AGENTS.md` worktree trap one layer out — resolution that works from a checkout and not from an install.**
  **Two defects, and the second is why the obvious fix would also have shipped broken.** Nothing for `chiefing` was packaged at all, which is the `@chief/chiefing/extension-runtime` half outright; and neither package's identity shipped, so even `piing`'s runtime — which WAS on disk — was unreachable by name. The trap: `piing`'s extension runtime is self-contained, but `chiefing`'s imports `../Errors.js`, `../transport/FetchTransport.js`, `../sse/SseWatcher.js` and a dozen more, so mirroring `piing`'s line would have packaged a directory whose every import escapes it and failed identically, in a release, after CI passed. `packages/chiefing/dist` ships whole.
  **The identity is a two-file shim per package, and both alternatives were rejected for stated reasons.** The real `package.json` would advertise entry points a release has no reason to carry — the "second answer waiting to be believed" the release script's own header warns about. A relative symlink would be invisible to `filesUnder`, which walks `resources/` to hash every file into the manifest and can neither recurse into a directory symlink nor hash it.
  **Nothing caught this because the only test of the packaging derived its expectations from the packaging.** `ReleaseArtifact.test.ts` walks `RESOURCE_SUBTREES` and asserts each entry was copied — an instrument agreeing with itself, structurally blind to a MISSING entry, and green throughout. The new guard takes its requirement from the other side: it reads the shipped extension SOURCES, extracts every `@chief/*` specifier they actually import, and asks **Node** to resolve each one from the directory an extension really sits in. Nothing in it derives from `RESOURCE_SUBTREES`, so deleting an entry from that list turns it red.
  **And the launch probe now asks the question it always meant to ask.** `runtime_lifecycle.rs` has probed since August that `dist/extensionruntime/index.js` EXISTS — a probe that passes on the exact tree that caused this outage, because the file shipped. **Existence was never the question; RESOLUTION was**, and the two differ precisely when the identity is absent. The new probe checks `<launcher_root>/node_modules/@chief/piing`, which is the one path correct for BOTH roots: a checkout's workspace link and an install's packaged shim sit in the same place relative to it. A bad install now refuses by name at launch instead of crash-looping into a pane that dies with its own evidence.
  Proven red-first under the CI shard's own conditions — `--ignore-scripts`, so no `dist` is built: the broken script fails with the operator's exact `ERR_MODULE_NOT_FOUND`, the fixed script passes, and a CONTROL arm proves a tree with every runtime file present and no identity still fails.
- **revert(upgrade): `GITHUB_TOKEN` support is removed from both release paths, because the need was the private period and the tree ships public.** Operator ruling: *"we're going to open-source the project, don't add shit that's not needed because of private."* The bearer header, `github_token`/`token_from_raw`, the by-id `releases/assets` download with its `Accept: application/octet-stream`, the `LatestRelease.assets` array, the two-state failure message, the `OPERATING.md` section, the `install.sh` header note and all thirteen tests are gone. `latest_tag` comes BACK, because it is what the anonymous path actually calls — restoring it is not resurrecting a shim, it is restoring the caller.
  **This is the repository's own no-stopgaps rule applied to a change that was argued past it.** The feature was justified on rate limits — 60 anonymous requests an hour per IP against 5,000 authenticated — which is a true fact that would have survived the flip, and it was still the wrong call: the reason the code was WRITTEN was a private repository answering 404, and a durable-sounding second justification for a change you already want is the most persuasive way to keep scaffolding. The honest test is not "is there a permanent argument for this" but "would this have been built without the temporary need".
  **Nothing was kept, and that was checked rather than assumed.** The one candidate — a clearer failure when the release list cannot be read — turns out to have existed already: `install.sh` said "(rate-limited, or no release yet)" before the feature, and `check()` already carried "a box behind a proxy or past GitHub's unauthenticated rate limit must be told plainly". So the ONLY thing the reverted message work added was the private-repository half. There was no token-independent improvement to preserve, which is the cleanest possible answer to "what did you keep and why".
  The consequence for the operator is real and is not hidden: while the repository stays private, `chief upgrade` and `install.sh` cannot see any release, and the 404 they get is indistinguishable from "no releases exist". That is the accepted cost of not shipping scaffolding, and it ends the moment the repository is public.
- **feat(actuator): every converge step says what it attempted, and a crash-looping person can no longer be shown a blank cause.** A live company would not start: six people, three crash-looping, `actuator.person.crash-looping` every five seconds for seven minutes with **`last_error` empty on every line**, `actuator.round.failed` naming only `step 3 tmux select-layout failed`, and **no step-level line anywhere** — no mint, no split, no person, no window, in failing rounds or passing ones. Nobody could name the fault because the actuator recorded nothing about what it tried. This change is the instrument, and it fixes no launch bug.
  **Every step logs its kind, its subject and its outcome.** `Step::kind()` and `Step::subject()` are one definition of the two words, and the interpreter writes `converge.step.begin`, `converge.step.ok` and `converge.step.failed` around every step it walks. A subject names the person by id, the window by tmux id (or by the symbolic id of the step that mints it) and the pane by id, so a failing round says WHO did not come up and WHICH window tmux refused, not only which index died. The refused and deferred lines carry the same two fields.
  **tmux's own words are kept, and a silent refusal still produces a cause.** The step detail was `stderr.trim()`, so a tmux that exits non-zero writing nothing reduced to an empty sentence. `refusal()` takes stderr, then stdout, then states the exit status — never nothing — and `StepError::cause()` is the one reader that turns any step failure into a sentence naming who said no.
  **`note_error` is wired from the spawn path, which is the gap that made every card blank.** It had exactly ONE caller and it fired only when the failing step was ITSELF a spawn step. A `select-layout` that tmux refuses is not a spawn step — so on the live company not one person was ever told a cause. The pass is fail-stop, so every spawn ordered BEHIND the failure was never attempted; those people are now named, and their card carries the other person's broken step and tmux's words. `CrashReport::cause()` replaces every direct read of `last_error`: absent, empty and whitespace-only all fall back to the pane-walk evidence, so a `crash-looping` line with no cause is not constructible. Pinned by `a_crash_report_can_never_carry_an_empty_cause`, `a_learned_sentence_is_what_the_cause_says` (`actuate/crash_loop/tests.rs`), `a_refused_step_carries_tmuxs_own_words_as_its_cause`, `a_silent_tmux_refusal_still_produces_a_cause`, `every_step_names_its_kind_and_its_subject` (`actuate/interpret/tests.rs`), and `a_failure_at_a_non_spawn_step_still_names_the_boots_it_cost`, `the_failing_round_names_the_step_it_attempted`, `a_failure_at_a_spawn_step_still_names_that_person` (`actuate/resident.rs`).
- **feat(chiefd): a company database is proof enough to restore a lost registry row at boot.** Operator repeal, 2026-08-26, after beacond's own store was destroyed externally under a live process and a real company sat unstartable behind a refusal that nothing could clear: *"chiefd should always try to register to beacond when it starts. if it exists, no-op. if it doesn't, register."* On `Admission::UnknownCompany`, chiefd now checks the one proof only a company controls — `<dir>/.chief/db/chief.db` exists on disk — self-registers through beacond's existing upsert (`ON CONFLICT(dir) DO UPDATE`; the `created` flag names which half ran), WARNs loudly on insert only (`beacond.company_row.restored` — seeing it every boot means the registry is LOSING rows, and the line is how that gets found), and retries admission exactly once. An empty directory still refuses — binding mints nothing — and a REMOVED company cannot resurrect: `chief rm` deletes `<dir>/.chief/` before the row, so the proof dies first. Mid-run behaviour is untouched: a heartbeat 404 is still the operator's deletion and is still never repaired (D22/F13). Pinned by `a_company_database_is_proof_enough_to_restore_a_lost_registry_row` (restore + slug-from-basename + loud-once across two boots) and the pre-existing `a_daemon_cannot_invent_a_company_by_binding`, both in `chiefd-daemon/tests/single_writer_admission.rs`.
- **fix(chief): a spawned daemon that dies is reported in milliseconds, in its own words — and the client stops guessing.** The wait loop REAPS its child (`try_wait`) on every pass instead of probing a pid: a child that has exited and not been reaped is a zombie, and a zombie answers `kill(pid, 0)` exactly as a running process does, which is why the died-child branch — promising the daemon's log tail since the day it was written — had never once fired for a spawned child. Measured in the outage this fixes: chiefd exited at +1.3 ms with a precise refusal already in `daemon.log`, the client polled the rendezvous 61 times over 15 s, and the operator was told to reinstall a binary that was the same build on both ends. Now a dead child fails in one pass with its exit status and the words it wrote ("it wrote this before exiting"), and the timeout message — reachable only while the child is provably ALIVE — leads with the log tail and offers the stale-install sentence as one possibility among named others, never as the answer. Pinned red-first by `an_exited_but_unreaped_child_still_answers_a_liveness_probe` (the OS-level premise, against `/bin/sh` and not our own code), `a_dead_childs_report_leads_with_its_exit_status_and_its_own_log`, and `the_timeout_message_shows_the_log_before_any_hypothesis` (the ordering IS the assertion), in `chief-cli/src/daemon.rs`.
- **chore(models): the dead model resolver is deleted, and the README stops claiming a capability that does not exist.** `README.md`'s feature list promised "Explicit skills, tools, and model choice for every hire" on the first screen a stranger reads. Chief does not choose models — that went with provider/model management and Pi owns it — so the line now reads "Explicit skills and tools for every hire; models are Pi's own."
  **`resolveModelForRequest` and `unknownModelMessage` are deleted with their tests.** Both were reachable only from the tombstoned `set_model` verb and had **zero production callers**: a complete, documented, tested model-resolution utility — three accepted spellings, ambiguity refusal, a carefully worded not-found sentence — sitting ready to be wired up. That is the reason to delete rather than leave: the repository rule is to remove obsolete paths instead of keeping them, and a working implementation in the tree turns "just let hire pick a model" from a decision into an afternoon of wiring. `MAX_LISTED_MODELS` went with them as their only reader, and the seam-classification rows that named them went too — a classification of deleted code is not a record worth keeping. A tombstone comment states what was removed and why, in the shape the file already uses.
  **`PersonModel`/`person_model.rs` is deliberately untouched.** It reads a person's effective model out of Pi's own transcript for the sleeping card. Reading is not setting, and the distinction is the whole point of the deletion.
- **feat(upgrade): `GITHUB_TOKEN` is an optional credential on both release paths, and a 404 stops claiming there are no releases.** `chief upgrade` and `install.sh` both read the releases API anonymously, so while the repository is private that endpoint answers **404 — the same answer GitHub gives for "this repository has cut no releases"** — and neither path could see `v0.5.0` at all. Both now send `Authorization: Bearer $GITHUB_TOKEN` when the variable is set and nothing when it is not.
  **The absent-token path is the default and is byte-identical to what shipped before.** No warning, no prompt, no degraded message — that is the normal case for every stranger, and a check that nags about an unset optional variable teaches people to ignore it. An empty or whitespace-only value counts as UNSET in both implementations, deliberately: `GITHUB_TOKEN=` in a sourced environment file is a variable somebody meant to unset, and sending `Authorization: Bearer ` would turn a working anonymous request into a 401 — the one way this option could break the path it must never touch.
  **This is not scaffolding for the private period and is not written as one.** GitHub allows an anonymous caller 60 API requests an hour PER IP against 5,000 authenticated. A shared box, a CI runner, or anything scripting installs shares that allowance with every other caller behind the same address, and exhausting it looks exactly like the release being missing. That reason outlives the repository becoming public, so the option is permanent and documented as permanent.
  **The download path needed more than a header, which is the part that would have shipped broken.** `github.com/<repo>/releases/download/<tag>/<asset>` is the BROWSER url and it does not serve a private release's asset to a bearer credential at any token — the authenticated route is `api.github.com/.../releases/assets/<id>` with `Accept: application/octet-stream`, and that accept header is load-bearing rather than decorative: the same endpoint answers any other accept with the asset's JSON DESCRIPTION, which would write a metadata document into the file the installer then verifies against SHA256SUMS. So `LatestRelease` gained its `assets` array and an authenticated download resolves by id, while an anonymous one keeps the browser url unchanged.
  **The failure message states two states and asserts neither.** A 404 reported as "no release exists" sends the reader looking for something that is sitting right there; the same distinction as `PiMissing` versus `PiUnusable`, where collapsing two causes into one message sends half the readers the wrong way. With no token it names the private-repository and rate-limit possibilities and the variable that settles both; with a token it says the opposite useful thing — expiry or access — because telling somebody who already set the variable to set it sends them in a circle.
  The token is only ever a header: never interpolated into a url, where it would reach `ps`, shell history and error output; never printed; and every error string this verb builds routes through the redaction already used for provider credentials. Thirteen unit tests pin the DECISIONS — which headers are built, which url is addressed, and the empty-value rule — in both directions and without a network.
- **chore(deps): seven dependabot bumps land as one branch, and the lint major was checked with an eslint the branch does not ship.** `eslint` 10.8.1 -> 10.9.0, `@eslint/js` 9.39.3 -> 10.0.1, `eslint-plugin-react-hooks` 5.2.0 -> 7.1.1, `uuid` 1.24.1 -> 1.25.0, `base64` 0.22.1 -> 0.23.1, `actions/download-artifact` 5 -> 8, `actions/setup-node` 6 -> 7. One commit per bump so a red bisects to a single dependency, both lockfiles regenerated rather than hand-edited, **and no bump dropped** — each major was read against its own release notes, and none is breaking here.
  **The stated risk was that a lint major silently DISABLES rules. Measured with both real binaries on the same file, the resolved rule set is IDENTICAL across the bump: 105 active before, 105 after, zero lost, zero gained.** A config diff returns a perfect null result. Twelve sites still failed CI. Two rules that the null result cannot see — `preserve-caught-error` and `no-useless-assignment` — account for nine of them, and `radix` accounts for the other three **without changing its name, its count, or its configured option**: `eslint.config.mjs` has said `['error','as-needed']` since long before this bump, and both versions report exactly that string, while eslint 9 calls `Number.parseInt(x, 10)` a REDUNDANT radix and eslint 10 calls `Number.parseInt(x)` a MISSING one. Same input, same config, opposite verdicts. **Comparing resolved configurations answers "were rules removed"; it cannot answer "did a rule change its mind", and on this bump the honest answer to the first question was no.**
  **The second half is worse and is the one worth keeping.** The local lint that produced the first fix ran **eslint 9.39.3** — resolved through a worktree `node_modules` linked back to a shared checkout installed from the OLD lockfile — while `package.json` and `bun.lock` on this branch both say 10.9.0. So the instrument was a version the branch does not contain, and it did not fail: it produced three confident, precisely-inverted edits, stripping the radix argument that the shipping version requires. CI rejected them with the OPPOSITE message at the identical three lines, which is the only reason it was caught. `bun install` did not fix it and reported "no changes" — the trap the repository already documents for `@chief/*` resolution reaches the toolchain itself, where it is quieter, because a linter of the wrong major still lints. **A dependency bump must be verified by the tool the lockfile names; check the version before you believe the output**, `eslint --version` being one command. The three radix edits are reverted; the nine genuine fixes stand.
  One method deviation recorded rather than smoothed over: `uuid` needed `cargo update -p uuid --precise`, beyond the two authorised commands, because `cargo metadata` will not move a dependency the existing lock already satisfies.
- **docs(agents): the window between a green board and the button, where a late push goes to die.** One clause on the verify-the-file entry, ruled after a ledger entry pushed AFTER a board went green was merged from the earlier head and reported as delivered — three of the four things in that commit had ridden and one had not. **Nothing in the author's view shows this**, because at the moment of the press the head was genuinely the head and the board was genuinely green; the reviewer's own headSha check passes correctly and cannot see it either. So the rule is the author's: after a merge you did not press, grep `origin/main` for anything you pushed near the green.
  **And check it by CONTENT, never by ancestry.** `git merge-base --is-ancestor` returns NO for every branch commit after a squash merge, so it cannot separate "did not land" from "landed normally". Run on that commit it gave the right answer for the wrong reason, and would have returned the same NO if all four pieces had ridden — **a fact that looks like the answer, is true, and is about something else**, which is the same instrument failure as trusting a mergeability flag that was true when read.
- **fix(docs): the ledger entries for the version reset were written by a helper that reproduced defect #890.** 63 entries vanished from `CHANGELOG.md` and `DECISIONS.md` — 23 and 40 — and the cause is the exact trap `doc-append-only.test.mjs`'s own header documents in its first four lines: `open(p,'w').write(new + open(p).read())` **truncates the file when the write handle is created, which happens BEFORE the read inside the argument runs**, so the read returns an empty file and the "append" writes only the new entry. The original incident wiped 4,784 and 4,706 lines across two merges and was found by a human reading a diff stat. **This one was caught by the guard that incident produced, on the first CI run, naming all 63 entries.**
  The repair is the one the trap requires: read into a VARIABLE first, then open for write. Worth recording as a class rather than a slip — the defect is invisible in the source because the expression reads left-to-right as read-then-write while Python evaluates the write handle first, and it survived four earlier ledger edits tonight only because each of those wrote to a file it had already read into a variable by a different route. **The instrument that caught it is the one the same mistake created**, which is the strongest available argument for keeping a guard whose subject has not recurred in months.
- **feat!: the first public release is `0.5.0`, and `package.json` is the version of record for the first time.** Operator ruling: the product is rough and 2.x claims a maturity it does not have.
- **chore(docs): the ROADMAP is deleted, and the two guards it was holding get durable homes.** Operator ruling. `docs/ROADMAP.md` and every LIVE reference to it are gone — `README.md`'s index row, the `docs/README.md` map row, `apps/web/README.md`'s revival pointer, and the `AGENTS.md` clause that named it as where the route-surface pin was recorded.
  **Nothing was lost to the tidy-up.** The file held one fact `docs/OPERATING.md` did not — WHY Developer-ID signing is not done, which is that it needs an Apple developer account and a human decision — and that clause is folded into OPERATING's signing paragraph, which already carried the ad-hoc-signing fact and the quarantine remedy. Every other upgrade row in the ROADMAP was a summary of behaviour `README.md` and `OPERATING.md` already document as live. The two post-launch guard entries — cited-name liveness, and the route-surface contract pin — are re-homed as GitHub issues with the reasoning they carried, the same ruling as the flip runbook: a durable home outside the shipped tree.
  **The acceptance grep is at zero over the LIVE tree and NOT over the ledgers, deliberately.** Three shipped entries in `CHANGELOG.md` and `DECISIONS.md` still name the ROADMAP, and they are append-only: those sentences were TRUE when written — the file existed and did carry what they say it carried — and deleting the file does not falsify a statement about the past. Editing them to make a grep read zero would be rewriting history to satisfy an instrument, which is the trade this release refused when it gave the ledger reset its own narrow mechanism rather than reaching for the exception registry. One of the three is now a DANGLING pointer rather than merely historical, and the correction for that is a new entry rather than an edit.
- **docs(actuate): the batch-abort property is written where somebody would trip on it.** The server input contract ships as ONE tmux client message — `start-server ; set-option … ; new-session` — and **an unrecognised option abandons the rest of the list, including the `new-session` at the end.** The result is not a degraded terminal; it is a company that does not start.
  Measured rather than reasoned, and both probes are in the comment: a bare `set-option` naming an unknown option leaves `no server running`, while the same option wrapped in `if-shell` reports the error and still creates the session. That `if-shell` idiom is already in this function, on the `terminal-features` line — **what was missing was any statement of WHY it is wrapped**, so the next person to add an option had to rediscover the reason by breaking session creation.
  This is a LATENT finding: true today, of code shipping today, and invisible until somebody tried to add a fourth line. Nothing in this repository exercises tmux's parsing of a command list we assemble, so no test would have caught it — the first symptom would have been a company that would not boot.
  The comment also carries the honest half, because a guard that hides a no-op is its own defect: `if-shell` makes an unavailable option SAFE, not APPLIED. On a tmux that lacks it the option is silently never set, so a guarded line must say which versions actually get it — otherwise it reads as though it always applies, **which is a check that passes without looking wearing different clothes.**
- **feat!(founder): Founder is the operator's own Pi with the founding skill ADDED, and the wheel scrolls again.** Two reported symptoms, two different causes, and only one of them was what it looked like.
  **The skills.** Four flags fenced Founder off from the operator's own installation — `--no-skills`, `--no-extensions`, `--no-context-files`, and a `--tools` allowlist of eight names. In Founder mode the `zipbox-browser` skill reported that it could not reach Chromium; it was not failing to reach anything, it was **never loaded**, and the allowlist would have filtered its tools out even if it had been. All four are deleted; `--skill founder`, the three extensions, `--approve` and `--name` stay, additive. The framing this restores is already the ledger's for the CEO — that is the OPERATOR's own Pi, not a managed agent — and Founder was the one pre-company session inconsistent with it.
  **The golden argv vector is REWRITTEN, not weakened.** Its subject was "the Rust port asks Pi for the same session as the deleted TypeScript", and asserting sameness would now be asserting the bug, because the divergence IS the ruling. It names the four fences and proves each absent, asserts the argv is the program plus exactly the additive flags, and asserts **every surviving element was in the ported shape too** — so this change is subtraction, and a new flag would need its own reason. The capture stays as the evidence of what the fences were.
  **The scroll, which was NOT the fences.** Founder runs inside tmux and plain `pi` does not: `chief` with no ambient session starts one on its own socket and attaches, and a pane with `mouse off` swallows the wheel — the terminal's native scrollback is not the pane's, and without mouse mode the wheel never reaches copy-mode either. The company session has always set `mouse on`, in the actuator's own bootstrap, on a **different server**. Nothing connected the two, which is how the session an operator meets FIRST became the only one where scrolling was broken. `create_operator_session` sets it now, pinned by a source-level test because the subject is a tmux server a test cannot mint. It deliberately does NOT take the company session's `status off` with it: that is a product decision about the operator surface rather than a bug fix.
  The two halves are separate commits because they are separate claims about separate causes. Shipping the fence removal as the answer to both would have looked complete and left the scroll report to come back.
- **docs(agents): an acknowledgement is not an artifact.** The verify-the-file rule assumed there is a fix in a tree somewhere to be mis-described. The sharpest case has nothing there at all: a ruling arrives, the reply agrees with it, and from that moment the author's own account has the item closed — while no file was ever touched. **Measured, and it is the tenth instance of the day's class:** a one-sentence ruling was relayed, acknowledged in a message, and reported complete across three successive heads. `git log --all -S` over every branch and reflog returned nothing, because it had never been written. Both offered explanations — a message crossing, and a commit lost to a rebase — were wrong, and the search is what settled it. **The smaller the ruling, the more completely acknowledging it feels like completing it**, because there is nothing in it big enough to feel like work. The remedy is the rule: a relayed ruling goes into the tree BEFORE the reply, and the reply quotes the file and the line — reply-then-implement is what produces this, and the gap is invisible because both feel like the same act.
- **fix(guards): section I.4 becomes a guard row, because the report of the zero was the last nonzero.** The entry announcing that the publication-gate placeholders were gone QUOTED the grep proving it — and was therefore the one remaining instance. **The run-the-guards-again rule could not reach this**, because that rule works by re-running guards and I.4 was not a guard: it was a runbook step, a grep to be run ONCE by a human at the moment of the flip, against a tree whose own changelog had quietly falsified it. It is an encoded row now, with no allowlist, red on every commit until the flip and forever after. The general form, written beside it: **a precondition that is only ever checked by a human at the moment of use is not a precondition, it is an intention. Where one can be a guard, it should be.** The entry names the class without spelling it, so it cannot re-offend.
  **And that row's first draft was filed in the wrong list**, which produced the worst failure shape in the file so far. It went into the SHAPE rules, which read a `pattern` field an encoded row does not have; the scanner stringified `undefined` and greped for THAT, reporting 1,785 leaks — every one an ordinary line containing the word "undefined". The failure was loud and pointed nowhere near its cause: nothing said "this row has no pattern". A missing field had become a regex matching a common English word, and **a scanner that fires constantly gets its row deleted rather than its bug found.** A new check now refuses a row filed in either wrong list, in both directions, naming which list it belongs in.
- **chore(oss): the operator's four answers, and the last publication-gate placeholder in the tree.** The placeholder grep that section I.4 requires to be empty before publication now IS empty — and that check has been promoted from a runbook step to a guard row, so it is answered on every commit rather than once by a human at the flip.
  **`.github/CODEOWNERS` is one line: `* @hishboy`.** Six placeholder team names and the header explaining them are deleted. The boundaries they described are real and documented where a reader can act on them, but a CODEOWNERS entry naming a team that does not exist silently matches NOBODY — worse than no file — and to a stranger it reads as a project ignoring its own review routing.
  **`SECURITY.md` commits to 5 business days**, in both the visible line and the comment that asked for it. **Dependabot is approved** as drafted — weekly, cargo plus npm plus actions, with the Rust toolchain and `@earendil-works/*` ignores kept.
  **The operator's given name is gone from twenty-six comments**, replaced with "the operator" — the house style `AGENTS.md` already uses. **Every WHY survives**: a comment reading `<name>'s mandate: every org-chart action is ONE fast SQL txn` becomes `The operator's mandate: …`, and the mandate, the incident and the ruling behind each citation are untouched. Only the byline changes. Two of the twenty-six were inside GENERATED artifacts deriving from one doc comment in `wire/common.rs`; the source and both artifacts move in the same commit, because a generated file out of step with its source is the drift class this release has been removing all day.
  **Two guard rows encode distinctions that would otherwise be discovered as breakage.** The maintainer's handle and a retired private hostname are the SAME SPELLING: the row now bans the bare form and never an `@`-prefixed owner reference, so the guard cannot refuse a file the operator personally approved. And the given name is banned CASE-SENSITIVELY — uniquely in this file — because the `lucy/*` eslint namespace is a ruled SURVIVOR and is the same letters in lower case. Both have controls asserting each direction, because a row that is wrong either way is a real failure: too loose ships a hostname, too strict refuses CODEOWNERS.
  **The name row caught its author TWICE while being written.** First the guard's own comment, which spelled the home path a neighbouring row bans. Then THIS ENTRY, which quoted the operator's name as its before/after example — caught by the re-run-after-the-ledger rule added to `AGENTS.md` hours earlier, on the first commit that rule applied to. That is the sixth and seventh time in this release a guard's first catch was its author, and every time the subject was fixed rather than the guard: **an explanation is not exempt from the rule it explains**, and the entry announcing a removal is the likeliest place for the removed thing to survive.
- **feat!(actuate): a relaunched pane receives NO synthesized text. The resume prompt is deleted, not gated.** Operator ruling: *"don't insert anything ever to anything. just boot the agent."* The reported symptom was a CEO created moments earlier that had never run, receiving the paragraph beginning "You were interrupted: your process was no longer running and was started again" — a sentence about a turn that had never happened, in a context that had nothing in it. `chiefd-core/src/runtime/resume_prompt.rs` is gone with every path that carried its output to a pane: the `resumePrompts` wire field on both declarations of the launch catalog, the `ResumeCause` key the client selected by, the `cause` parameter threaded through `launch_command` and `pane_command`, and the workspace test crate that existed only to drive the composed sentence through tmux quoting — with its member row, CI lane and manifest entry. `push_launch_flags` is module-private again, because that crate was its only reason to be public. DELETED, NOT GATED: no flag, no empty-string default, no "only when the pane really was replaced". A flag would leave the machinery alive for somebody to switch back on.
  **THE FOUNDING GREETING STAYS, and that is a ruling and not an oversight.** A first boot with no transcript still receives `fresh_session_message`, whose `Founding` arm tells a brand-new CEO to introduce itself and *"create no department, hire nobody"*. The operator kept it: the reported symptom was RESUME copy leaking onto a first boot — which is what this change deletes — and the greeting is what a first boot was always meant to get. Its own doc comment records why it exists, and the reason is live: without it the operator watched a company they had just created start building departments and hiring into them while they were still reading the first screen. It is pinned by `the_founding_boot_is_told_to_introduce_itself_and_build_nothing`, which is also the citation under the README's Founder claim.
  **Every deleted test's subject was the deleted feature, and every test whose subject was something ELSE keeps all of its assertions.** That distinction was the whole risk: `crash_loop_never_gives_up` pins the no-deadline invariant and merely happened to build a `LaunchSpec` with an empty prompt map, so it loses one fixture field and not one assertion — verified by assertion count, unchanged in all five flagged files. One survivor whose NAME resembles a resume test keeps it, with a comment saying why it survived a deletion it looks like part of.
  **And the absence is PINNED.** `a_resumed_pane_is_handed_its_session_and_no_synthesized_text` asserts a resumed pane still carries `--session` — this bans invented copy, not resume — and that the session path is the LAST argument on the line, because anything after it is a positional prompt, which is the shape the deleted feature had and the shape any regrowth would take. "The agent doesn't know what happened to it" is a reasonable thing to notice and injecting an explanation is the reflex fix, exactly as it was the first time.
- **fix(eslinter): a RETIRED rule must PROVE its subject is gone, not merely claim it.** `RETIRED_RULES` was introduced so a deletion could not quietly shrink a historical count, and its premise — *the rule's SUBJECT left the tree* — was stated only in prose. An unchecked premise makes the category a place a deletion can be FILED rather than justified: retire a rule whose subject is alive and the tree silently loses a live check. Each row now names the pattern its rule can only fire on, asserted to match nothing in tracked source. It is the mirror of the redaction guard's survivor rows, which assert their terms are still PRESENT — the same liveness idea from the other side, and worth more taught in two places than in one.
  **The assertion earned its place on its first run**, by rejecting the pattern I gave it. A bare `bignumber` reported three files: a cross-reference in a sibling rule's comment, a message string in an unrelated rule, and this file naming the pattern in order to check it. None is a subject either rule could fire on. The subject is the IMPORT — one rule inspects the import declaration, the other a BigNumber value, which requires the import to exist — so the pattern is the module specifier and the check passes on a true premise rather than on a widened one. Widening it to cover prose would have made the check unpassable for a reason unrelated to the claim, which is the failure this whole release has been about in its other direction.
- **chore(deps): every unused dependency is removed, on both sides, and the LGPL transitive goes with them.** NINE TypeScript packages had zero importers anywhere in `apps`, `packages` or `scripts` — `@solana/web3.js`, `@solana/spl-token`, `viem`, `@privy-io/node`, `bignumber.js`, `bs58` (with `@types/bs58`), `commander` and `jose`. The motivating finding: `@solana/web3.js` pulled `rpc-websockets`, which is **LGPL-3.0-only**. Nothing shipped it, so there was no distribution problem — but it has no business in the tree of a repository going public. It is gone from `bun.lock`. ELEVEN Rust dependencies were declared and never named in their crate's source: `tracing-subscriber` in `beacond` (which delegates to `chiefd_log::install`) and in `chiefd-daemon`; `hyper` and `hyper-util` in `chiefd-api`, which spells neither; `uuid` in `chiefd-core` and `chiefd-daemon`; `insta` in `chiefd-core` and `chiefd-host`; `proptest` in `chiefd-core`; `notify` in `chiefd-host`, whose subject — the root Pi provider registry watcher — went with provider/model management; and a dev `tempfile` in `chiefd-log`. `proptest` and `notify` were then orphaned in `[workspace.dependencies]` and came out with them. `Cargo.lock` loses 34 crates. **The ignore lists came out in the SAME commit**, because an ignore that outlives its subject is dead config and knip is a CI gate: nine rows left `knip.jsonc`, which is what makes the removal durable rather than tidy — knip now refuses a silent reintroduction. TWO ESLINT RULES LOST THEIR SUBJECT and are deleted rather than kept as decoration — `enforce-bignumber-default-import` and `no-bignumber-to-string` both fire only on `bignumber.js` imports and BigNumber values, and a lint rule whose subject is not in the tree reports green by seeing nothing. `RuleCatalog.test.mjs` gains a third category, `RETIRED_RULES`, so the historical finding it locks stays TRUE: 32 kept + 14 dropped + 2 retired still balances to the 48 the port started from. A count that quietly shrinks to match a deletion matches nothing anybody decided. **And a retired row must PROVE its premise**: each carries a `subjectPattern` asserted to have zero tracked-source hits, so a rule cannot be retired while its subject still exists — the mirror of the redaction guard's survivors-assert-presence idiom, which is now the same liveness idea taught in two places. The root eslint config's `viem`/`zeroAddress` restriction went with the dependency for the same reason.
- **docs(guards): run the guards again after writing the ledger entries, and say what the append-only guard can SEE.** Two statements about the limits of a check, which is the one thing a green board cannot tell you.
  `AGENTS.md`'s guard-selection entry gains the sentence its own branch proved: **the CHANGELOG and DECISIONS entries are the last files written and therefore the only ones a pre-push run never saw — and an entry describing a pattern the change removes will contain that pattern.** That is not specific to one sweep. The entry is always written last, and describing what a change removed means spelling the removed thing; a redaction guard went red in CI on the very entry announcing that its class was at zero. One re-run closes the gap the rule above otherwise leaves open.
  `doc-append-only.test.mjs` gains its OBSERVATION WINDOW in writing. On a pull request CI checks out the synthetic MERGE commit, whose parents are `main` and the branch head, so intermediate branch commits are never any checked commit's parent. A first-line edit made BETWEEN two branch pushes is therefore structurally invisible to this guard — not missed, invisible, by the shape of what CI hands it, and widening the comparison logic cannot reach commits that are not in the graph. It is named rather than left implicit so nobody reads a green as a statement about branch-internal ledger history: what protects that history is REVIEW, reading the file at the pushed SHA, and not this file. **A reviewer who assumes otherwise is trusting a check that was never asked the question.**
- **fix: the guard caught its own CHANGELOG entry, for the fourth time in this release, and the entry changed.** The entry describing the 139-citation sweep QUOTED concrete plan filenames as its worked examples — so a commit whose subject was removing that class reintroduced two instances of it. The examples are generic now. That is the fourth time in this release a guard's first catch was its own author, and the fourth time the subject was fixed rather than the guard: **an entry explaining a ban is not exempt from the ban.** Also in this commit, the new resolve-at-the-ask test is brought to the house lint: the registry stand-in uses the named `ModelRegistry` type with a scoped assertion carrying its reason — the same trade `RuntimePaneIdentity.test.ts` already makes for `spawnSync`, because structurally satisfying a twenty-member class the code under test ignores would be a second, always-stale copy of a shape nobody reads.
- **test(intercom): the resolve-at-the-ask rule is pinned, closing one of the two recorded gaps.** `ModelIdResolvesAtTheAsk.test.ts` pins that a model id is resolved against **Pi's own registry at the ask** — refreshed and read per call, never validated against a chief-side list, which is the deleted allowlist growing back — and that provider and model are **never joined with a slash**. It covers all four spellings from the live incident that motivated the resolver: the right id, the same without its date suffix, the same with a provider glued on the front, and the bare id paired with the VENDOR as though it were the Pi provider. Ambiguity REFUSES rather than guessing, because picking one is a silent model change. The slash case asserts the exact rendering that was once investigated as a doubled-prefix concatenation bug that does not exist — which is what an unpinned rule costs: somebody's afternoon, twice. A unit test on an existing rule changes no behaviour, which is why this rides the release rather than the post-launch queue. The ledger records the closure as a NEW APPENDED entry rather than by rewording the entry that named the gap — a shipped entry is never reworded, and that is the same rule the public-ledger reset needed its own narrow mechanism to avoid breaking. The second gap, that observation never flows upward, stays recorded: `AGENTS.md`'s placement section is the standing rule until the route-surface contract pin on the roadmap exists.
- **chore(oss): 139 citations of unpublished plan documents are swept, and a guard row keeps the class at zero.** `plans/` is a LOCAL working document and no longer ships, so every concrete plan filename left in the tree was a pointer at something no reader can open — 139 of them across 91 files, 40 Rust, 19 TypeScript, 10 mjs, 9 toml, 7 md. The rule applied 139 times: **drop the POINTER, keep the sentence.** a comment reading `Stage 4 of <a plan filename> deletes that reap` becomes `Stage 4 of that work deletes that reap`; where the citation IS the sentence, the sentence goes. Concrete plan filenames tree-wide are now at ZERO.
  Two things about HOW, because the first two attempts were wrong and the reason is reusable. A blanket tidy-up of the whitespace each deletion left rewrote 17,000 lines and ate the checkboxes out of the pull-request template; the fix is that a rewrite rule may only touch the span it matched. And a sweep that treated every occurrence alike rewrote PATH STRINGS INSIDE TEST FIXTURES — an assertion comparing a changed-path list against a plan-shaped path is a synthetic fixture proving a classifier's behaviour, not a citation, and rewriting it changes what the test means. The sweep is therefore restricted to COMMENT lines, and the data sites were handled one at a time.
  Three of those data sites turned out to be DEAD CODE, which the sweep found by making somebody look at them. `ci-pr-scope.mjs` admitted any path under `plans/` as documentation so a plan diagram could skip the CI matrix; `plans/` is git-ignored, so no such path can appear in a diff and the clause could never fire again. It is deleted, and the two tests whose only subject was that clause are deleted with it rather than weakened to pass — with what they were really protecting rewritten to survive: an image is never documentation whatever directory it sits in, and a leading space is preserved so a path cannot be laundered by whitespace. `no-ts-cli-stub`'s prose-directory list and `suspected-done-issues`'s fixture had the same dead entry.
  The guard gains the row that keeps the class at zero: `plans/[a-z0-9][a-z0-9-]*\.(md|png)`. **It needs no allowlist by construction** — the sanctioned placeholders `plans/<slug>.md` and `plans/**` cannot match a pattern requiring a lowercase letter or digit after the slash, so the safe case is UNMATCHABLE rather than excused. Prefer that shape over a rule that needs a list of exceptions: a list rots, an impossibility does not. The row's first catch was its own fixture, which spelled a real plan filename — the third time in this release that a guard's first victim was its author, and the third time the fixture was fixed rather than the guard.
- **docs: the README stops claiming Founder designs the company, and three rules that were only lessons become entries.** README:49-52 taught the pre-correction shape — "tell it what company you want and it builds the org chart". Founder learns exactly two facts, the name and the purpose, and deliberately designs nothing; the CEO is what builds the organisation. The correction had landed once, in `examples/README.md`, while the README's first screen kept the old shape — a fix applied at one site without a sweep, which is the same failure as matching only the qualified form of a hostname. **The rewrite CITES ITS PIN** — `the_founding_boot_is_told_to_introduce_itself_and_build_nothing`, which asserts the CEO's first message carries "Create no department, hire nobody" — so the claim decays loudly if the assert ever moves. `FounderComposer.tsx`'s placeholder is corrected too: `apps/web` being non-live is invisible to a stranger reading it. Also: "two programs" becomes "two programs you meet, plus beacond", because the installer puts three binaries in `~/.chief/bin` and the first screen must not be untrue about what lands; and "found in one command" becomes "found in a minute", because one command plus two pastes must not be the first promise the product fails.
  Three rules that had been living as lessons are now entries. `AGENTS.md`'s placement section gains the ONE-WAY clause: the client derives and projects and never reports observed runtime state upward, the single upward route is an agent reporting about ITSELF, and a route carrying what a CLIENT saw is wrong by design — written there because it cannot be pinned today, with the reason recorded rather than a decorative citation invented. A second entry states the guard-selection rule as a CLASS: **run the guards that NAME the file you edited, not the guards whose SUBJECT you edited**, with the three instances this release produced — an append-only ledger where an entry's first line IS its fingerprint, this file where a struck exemption is a rule an agent will otherwise obey, and a triage narrative whose sentences carry counts another file must agree with. Documentation is not a safe place to edit. And beside it, the size-floor class: a floor encoding yesterday's tree size fails the day the tree legitimately shrinks, and the failure reads as a regression rather than a stale calibration. `docs/ROADMAP.md` gains the two post-launch guards — cited-name liveness, and an exact-enumeration route-surface contract pin — so both survive `plans/` being gone.
- **fix(redaction): three residual leaks, and the guard stops republishing in its own prose.** An acceptance grep found what a completed sweep had left: the operator's messaging chat id survived in `docs/store-implementation-audit.md` PAIRED WITH A CONTACT NAME, one document away from the `diagnostics.rs` fixture it had just been removed from; a macOS `/Users/<name>` fixture in `chiefd-log` and a piing test carried a real name past a shape row written only against `/home/`; and the redaction guard's own HEADER spelled a real hostname as its example of what a reader reconstructs — ruling one defeated by the file's prose, which the self-test missed because it checked decoded literals and not the comment. The home-path rows now cover BOTH platform spellings from one definition; the chat id and the contact name are rows of their own, encoded; the header example is fictional. **And the self-test now asks each row's OWN question rather than a substring question**, sharing `bannedRegex` with the scanner. A raw substring test reported this guard for four letters of a short contact name sitting inside the word "republish" — a false positive whose obvious repair is to weaken the check. Sharing the predicate is the real repair: if a row would not flag that text in the tree, it must not flag it here, and two copies of a predicate is how the two answers start disagreeing.
- **fix(docs): the parked-corpus narrative states its e2e cluster count again.** Clearing a dead reference in `docs/testing/parked-suite-triage.md` also removed the only line where the word "e2e" and the number 3 sat together, and `parked-suite-triage.test.mjs` rule 6 reads exactly that coupling — the `.md` narrative's cluster counts must match the JSON row counts, checked by a regex over one line. The sentence now says so directly rather than incidentally. The reading is worth more than the fix: a documentation edit broke a COUNT guard, because the prose was load-bearing in a way nothing in the sentence announced. Running the guards whose SUBJECT you edited is not the same as running the guards that NAME the file you edited, and this was the second.
- **feat(guards): two ABSENCE pins, and the carried ledger now names a test per entry.** Every other rule in this repository is pinned by a test that watches something HAPPEN. Two are not, and an absence has no natural witness — the code that would violate it does not exist yet, so both rules lived in a single doc comment, which is prose a future change reads past. `no-typing-at-a-live-agent.test.mjs` pins that no product path types keystrokes at a pane: the discriminator is the tmux flag, because `-M`, `-H` and `-X` are mouse forwarding and copy-mode while everything else is typing, so the guard does not fire on the comments explaining it — including its own. Its allowlist is EMPTY, and that was itself a finding: it opened with the founder bootstrap on the belief that it types into the operator's outer terminal, and the production half of that file contains no keystroke call at all. The row came out rather than being kept as documentation, and the liveness test that caught it is the one that will catch the next. `discovery-has-no-sweeper.test.mjs` pins that beacond never expires, sweeps or reaps a registration on a timer. Banned by SHAPE — a periodic driver, an elapsed-time comparison, a TTL constant — because nobody adding one will call it a sweeper. A TTL is the reflex fix for the first stale row anybody sees and is wrong twice over: the row may belong to a company that is slow rather than dead, so deleting it makes a healthy daemon unreachable to every client, and a sweeper is a background loop the reactive mandate bans outright.
  `DECISIONS.md`'s carried set was reworked against the same bar. A decision a NAMED GUARD already enforces is no longer carried — it lives at its guard, and a second copy is a second thing to keep true — and **every surviving entry NAMES ITS PINNING TEST**. An entry that cannot name one is not a decision that binds the code; it is doctrine, and doctrine belongs in `AGENTS.md` or nowhere. Two entries name a GAP instead, deliberately and in the entry: that observation never flows upward has no discriminating route-surface assertion, and that a model id is resolved against Pi's own registry at the ask has no test outside the extension itself. Recording a gap where a citation belongs is the honest form; inventing a test to fill the column would have been the dishonest one. One carried entry was WRONG at birth — it stated a fixed rail width that a later ruling had already replaced with an operator-owned width — and was fixed by rewriting the commit that introduced it, so the wrong first line never existed in any commit rather than being corrected in a second one.
- **docs: the day-one dead references are cleared, and AGENTS.md stops teaching agents to accept reds that cannot happen.** A two-shape walk — markdown links AND prose-named paths, the second being the shape a link checker misses — found fourteen live dead references one hop from the README. Each is DELETED rather than restored: the design record they pointed at is unpublished, and a public README must not point at it. `apps/chiefd/README.md` loses two 404 links and three prose citations; `conformance/README.md` and `FORMAT.md`, `packages/piing/README.md`, `docs/verification-rules.md`, `docs/ORGANIZATION_ARCHITECTURE.md`, `docs/pi-patch-capability-losses.md` and two `ci.yml` step comments lose theirs. `docs/commit-namespace.md` documented a lint script that does not exist and that no CI check replaces; the section now states plainly that the convention is carried by review and that this is the settled position, not a gap somebody should fill. `docs/testing/parked-suite-triage.md` loses its eight citations of a sibling document that went with the E2E corpus. `docs/README.md` claims to list every document and now actually lists the two it omitted. `docs/testing/TEST_SUITE.md`'s absolute skill paths are parameterised with an explicit "operators: substitute your own" note — the steps are the contract, the paths are not.
  In `AGENTS.md`: the `sql-only-state` git-identity exemption is STRUCK, because that guard now sets `GIT_CONFIG_GLOBAL` and the exemption has no subject; two of the three named "known refusals" are struck because those guard files no longer exist. An entry that teaches an agent to accept a red which cannot occur is the dead-red-hides-a-live-one failure that whole section is written against, committed by the section itself. The wake-lease entry gains the clause that was living only in a code comment: the OBSERVED window is the lease plus the reconcile pass's sampling floor, so a stopwatch reads longer than the constant every time, and "correcting" that by shortening the lease is a change to the operator's ruling wearing a bug fix's clothes. And a new standing entry — **verify the FILE, never the sentence about the file** — records the three occasions in one day when a completion report described the fix its author intended rather than the fix the tree carried.
- **fix(guards): the tree floor is DERIVED, the ledger reset asserts replacement, and the redaction guard stops republishing what it bans.** Three rulings, all of them corrections to work that had reported itself done.
  - `guard-tree-purity`'s non-vacuity floor no longer carries a number. It reads `snapshot.size >= trackedCount`, with `trackedCount` from `git ls-files` at runtime and refused in words if that cannot be read. Every tracked file must appear in a healthy walk and directories only push the size higher, so the assertion is true of every correct walk at every repository size — including the day somebody deletes another thousand files. A literal `2000` failed a correct snapshot once; replacing it with `1000` was a better-documented magic number, and the comment even instructed the next reader to repeat the maintenance the assertion had just failed at.
  - `PUBLIC_LEDGER_RESET` now ASSERTS the replacement instead of declining to compare. Not one entry of the pre-reset ledger may survive into the new file: a decision that still binds is RESTATED as a new dated entry, never copied. Without that, the reset commit was a one-commit window in which most of the old ledger could be kept while a few shipped entries were quietly reworded — the very thing the guard exists to stop, reintroduced once. It also REFUSES in words on states the design does not cover: more than one sentinel-prefixed entry, at HEAD or at a parent, is not silently read as "reset". Four fixture proofs, two of them red-first.
  - The redaction guard was republishing the private fleet. Its rows spelled every machine name in split string literals and stated each machine's ROLE, which turns a list into a map; the split defeats the guard's own grep and defeats nothing else. Rows are now SHAPE FIRST — an address at an internal mail domain, a hostname under the fleet domain, a routable IP literal — which ban a form without naming anything and catch the NEXT leak. The shapeless residue is stored base64 and decoded at runtime, with deliberately generic reasons, and a test asserts no decoded name appears in the guard's own source. Two narrowings are pinned so nobody "completes" them into noise: a hostname that is also an ordinary word is banned only in its FQDN form, and IP literals are allowed in the loopback, RFC 1918 and RFC 5737 documentation ranges.
  The new rows immediately found NINE more leaks the first sweep never looked for — three more private hostnames across a parked test corpus, two Rust comments, two sweep scripts and a piing test — all generalized in place. The guard also caught its own fixture spelling a routable address; that is now composed from octets at runtime. In the same pass the operator's real chat id came out of `diagnostics.rs`, where it was the fixture for the redaction function itself, `converge_safety.rs` stopped describing a retired database path, and `prepare_ceo_only`'s doc comment stopped contradicting its own pinning test about whether the CEO parks.
- **fix(guards): a second non-vacuity floor was calibrated against the private ledgers, and is now size-independent.** `append-only-duplicates.test.mjs` asserted that its entry pattern matched more than 100 lines per file. The count was standing in for "this is really the file", and that proxy died with the public-ledger reset: both ledgers legitimately start at a handful of entries, so a correct pattern reading a correct file went red. The replacement asks the same question without knowing the file's size — the pattern must match SOMETHING, and it must match the file's very FIRST top-level line, which is the one line a still-correct pattern cannot miss. The "all, not many" half was never this test's job anyway; the coverage test beside it already asks that, and asks it better. This is the same class as the `guard-tree-purity` floor recalibrated earlier in this release, and the reading is the same one: a floor that encodes yesterday's SIZE fails on tomorrow's correct tree, so a floor should encode the PROPERTY it is really testing.
- **chore(oss): the hostname sweep is finished by GREP, and a repo guard now keeps it finished (E.2).** The previous sweep matched only the fully-qualified form of a fleet hostname, reported completion, and left every BARE hostname in the tree — 74 occurrences of one box name, one of them in a file that sweep had itself named as generalized. That is what this entry is really about: a redaction whose acceptance is "the files I edited" is a diff summary, not a redaction. Six identifier classes are now at zero across the whole tree — three private box names (74, 11 and 9 occurrences), the internal fleet domain, a contributor namespace from the private repository (1,621 occurrences, all inside a deleted branch-sweep receipt), and an operator home path in a Rust fixture (3). Prose was generalized rather than deleted: every incident keeps its fact, its date and its numbers, and only the machine name goes. Terminal transcripts keep their shape as `root@host`. One operator quotation takes a bracketed editorial substitution rather than a silent rewrite, because changing words inside quotation marks is not redaction. `scripts/test/no-private-identifiers.test.mjs` is the new guard. It greps the whole tree on every commit; each banned row carries the reason it is banned, and each ruled-on survivor carries the ruling that kept it plus an assertion that it is still PRESENT, so an allowlist row cannot outlive its subject. It is wired BY FILE in `guard-wiring-manifest.mjs`, the way this repo wires guards, and it covers the two fresh ledgers from their first commit with NO exception carried — it caught the first draft of this very entry, which named the hostnames it was reporting as removed. Four non-vacuity proofs run against real throwaway git trees, and one plants the BARE form specifically, because matching only the qualified form is the measured miss this guard replaces. `DECISIONS.md` also gained twenty-five carried-forward entries: the decisions that live ONLY in the archived private ledger and still bind the code, restated with the reason somebody would otherwise undo them. Anything `CLAUDE.md` or `AGENTS.md` already states was deliberately NOT copied — two sources for one rule is how a rule starts drifting.
- **chore(oss): the two ledgers start fresh for the public tree, and the append-only guard learned a one-time reset (E.2).** `CHANGELOG.md` and `DECISIONS.md` had grown to about 16,800 lines of internal narrative — detailed incident write-ups, named hosts, and roughly thirty-six leaked machine names — written for the people who were in the room. Both now start fresh; the private ledger is archived, not published, and what still binds the code is carried forward as new dated entries in `DECISIONS.md` rather than lost. The rejected alternative was a mass rewording of shipped entries through `doc-append-only`'s per-entry exception registry, which would have taught every future contributor that an append-only file may be reworded when the reason seems good enough — a repeal of the rule wearing the rule's clothes. `doc-append-only.test.mjs` instead gained `PUBLIC_LEDGER_RESET`, a separate and narrower concept: a parent's entries are skipped for one file only when the file at HEAD carries a named sentinel first line and that parent does not. Detected by CONTENT, never by a commit SHA, so it survives the squash merge it lands through; SELF-LIMITING, because from the next commit onward every parent carries the sentinel too and ordinary append-only owns both files again with no exception row left to rot; and a second reset would need somebody to edit the constant with a new sentinel in a reviewed diff. Nothing else was relaxed — malformed prefixes, glued entries and the non-vacuity refusal all still run, and the skip is RECORDED in the check's own report rather than taken silently. Three fixture proofs pin it: the reset passes and names how many entries it skipped, dropping an entry AFTER the reset is a violation again, and wiping the ledger WITHOUT the sentinel is still the #890 failure. In the same pass the internal one-off artifacts left over from E.5 were deleted — `docs/branch-audit/`, `docs/fleet-handoff.md`, `docs/merger-landing-record.md`, `docs/infra-surface-findings-eng2.md`, `HANDOFF.md` and the numbered `docs/testing/` analyses — and the five stale pathspec exclusions those deletions orphaned came out of `chiefd-workspace-location.test.mjs` with them, 9 entries down to 4.
- **The public changelog starts here.** chief was developed privately for months before this release, and the changelog of that private development is not published. It still exists — it is kept in the private archive, not lost — but it is an internal narrative written for the people who were in the room, and it names hosts, incidents and colleagues that no outside reader has any business reading. This file records delivered behaviour from the open-source release onward, newest first. It is APPEND-ONLY: a shipped entry is never reordered, reworded or tidied, and a correction is appended as a new newest entry. `scripts/test/doc-append-only.test.mjs` enforces that against every commit.
